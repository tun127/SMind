node_modules
out
dist
release
# 测试构建（-rt.N）的输出目录：只在本机验证用，版本号带 -rt. 且从不上传。
# 单独列出来是因为它不在上面的 `release` 忽略范围内，忘了会连 113 MB 的包一起提交。
release-rt
# 真机验收用的两个大体积目录，都不进版本库：
#   SMind/          = 实际安装目录（D2 真机验收时把 0.9.2 装在这里，含 SMind.exe 与卸载器，约 376 MB）
#   release-verify/ = 复验用的独立打包输出目录（约 592 MB）
# 忘了忽略的话，谁一个 git add . 就会把近 1 GB 的产物提交进去。
/SMind/
/release-verify/
.DS_Store
*.log
.vite
tsconfig.tsbuildinfo
.tmp-check
.npm-cache
dev.log
dev.err.log
# 排查用产物：lint 的 JSON 输出、从 exe 反向提取图标用来核对的图
lint.json
.icon-check.png
# 商业计划（定价 / 路线 / 文案）：仓库是公开的，这些不进版本库。
# 若要把它们纳入版本管理（比如放私有仓库），删掉这一行即可。
commercialization/
.dsh-meow
# AgentTeams 的运行期状态目录（团队成员邮箱/任务快照）：属于本机运行态，不进版本库
.agent-teams
# 许可签发的私钥：永远不进仓库（默认落在仓库之外，这里是第二道保险）
license-private.pem
*.license-key
# 上传凭据（OSS / R2 / Cloudflare 等）：本机载入的环境变量文件，永远不进仓库。
# 这个仓库是**公开**的——一旦提交，密钥等于永久泄露（git 历史里删不干净），所以宁可多列几条。
*.env.ps1
*.env.sh
.oss.env
.r2.env
# 用文件传中文提交信息时产生的临时文件（误提交过一次）
.commit-msg.txt
# 开发版的输出日志（排查「窗口是不是旧的 / 崩没崩」时用）
.dev.log
# V8 采样产物（SMIND_PROFILE=1 排查卡死时会在项目根目录留下）
isolate-*.log
