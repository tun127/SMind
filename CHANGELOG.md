# 更新日志

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)；
每个版本都对应一次**可运行、可验收**的交付（验收步骤见 [`docs/P1-acceptance.md`](docs/P1-acceptance.md)）。
内容依据提交历史整理，未做美化。

## [未发布]

### 优化 · 视口与渲染（四项视口修复 + AI 回合内的布局节流合帧）

- **滚轮 / 触控板平移不再逐事件重渲染**：每个物理事件都写一次 pan（实测 120–160 次/秒），
  每次都把整棵画布重渲染一遍——滚动时主线程被打满，同期运行的 AI 批量写入会被饿死。
  现在把位移累进局部变量、`requestAnimationFrame` 每帧最多落账一次（右键/中键拖拽同理），
  落点与逐事件处理完全一致。
- **复活 `TopicNode` 的 `memo`**：以前传给节点的 8 个回调每次渲染都是新函数，浅比较必然失败，
  memo 整个被架空——每次 pan 更新都放大成「全部可见节点重渲染」。回调提为稳定引用
  （实时值改走 ref）。
- **视角锁定不再和用户的手抢镜头**（用户报告的「画面一长就上下抽动一阵再恢复」）：
  跟随循环每帧把镜头往焦点主题拽 22%，而**滚轮滚动没有任何让位规则**（拖拽有），
  两股力对拉 → 抽动，直到 240 帧止损才停。现在滚轮 / 拖拽 / 缩放 / 居中 / 适应画布都算
  「用户接管视角」，跟随循环当帧让位；下一次选择或几何变化再重新咬住（与拖拽后的行为一致）。
- **折叠之后镜头不再失去中心**：被折叠收起来的选择已不在布局里（`nodeMap` 查不到），
  跟随循环会一直等一个永远不来的目标、90 帧后静默放弃——现在回退到中心主题。
- **AI 回合内的布局节流合帧**（`usePacedWorkbook`）：AI 批量写入时把重排合并到每 ~100ms 一次；
  节点文字/选中/悬停仍实时读最新数据，只是位置晚一拍。用户自己的操作不在回合里，零延迟。
- 视角锁定的三类异常告警改为**同类只报一次**：它们处在每帧路径上，而渲染层 `console.warn`
  会被主进程转发进应用日志（同步落盘）——持续异常时会变成每秒几十次写盘的**新卡顿来源**。

### 修复 · 切换文档时「对话消失、但回合还在跑」（跨文档误写）

- 回合状态（轮数 / 消息线 / 待执行队列）都在聊天面板的 ref 里，**不随文档走**；
  而文档路径一变（首次保存 / 恢复 / 另存为）面板就会清空或加载新路径的历史——
  于是界面上「对话消失了、但还在第 14 轮」；更糟的是写工具作用于**当前激活文档**，
  继续跑等于可能把改动落到另一份文档上。
- 修：文档一变就**中止进行中的回合**（取消流、清队列与待确认、结算撤销事务、写日志），
  然后按新路径加载它自己的历史。消息不再无声消失，跨文档误写也被堵死。

### 安全 · API Key 不再以明文留在配置文件里

- 写入改用**系统安全存储**（Windows 走 DPAPI，密钥绑定当前用户账户）：`ai-config.json` 里
  只有密文 `apiKeyEnc`，**不再出现明文 `apiKey`**。
- **读取兼容老文件**，并在启动时自动迁移一次——升级不会让用户的 Key 失效（迁移幂等，
  只对存在明文的文件动手）。
- 换了机器/账户导致解不开时，当作"没有 Key"让用户重填，而不是抛错把面板打挂；
  Linux 没有 keyring 时退回明文（能用比"安全但不能用"重要）。

### 新增 · 三期的上下文压缩（长对话不再「失忆」）

- 以前是**截断最近 16 条**：模型会忘掉之前干过什么，用户说「继续」它就从零重读一遍导图
  （大导图上就是把同一种折腾重复一遍）。
- 现在：**最近 6 条保留原文**，更早的折叠成一段「此前做过什么」注入消息线。
- 摘要里的「我做了…」**取的是工具条目（执行记录）**，不是模型自述——自述会说错，记录不会。
- **零模型调用**：纯函数 `compressHistory`，确定性、可自检（新增 14 项断言）。
- 摘要超长时从**最早**丢（越近越重要）；注入时带一句「别凭这份摘要去改节点，细节请重新探查」
  ——因为本项目的判断是「**文档本身才是 agent 的持久记忆**」，对话可以激进压缩。

### 修复 · 致命：代码块里出现 `@` 或 `\` 会让界面**永久冻死**（渲染进程 100% CPU）

- **现象**：AI 批量补全 Python 代码块后，往画布**下方滚动、滑到一半整机卡死**；
  日志里只有 `unresponsive`，只能强杀进程。小文档（23 个节点）同样稳定复现，
  而且**不调用 AI、只恢复文档再滚动**也会卡死。
- **真凶**：分词器标识符分支里 `IDENT_START` 认 `@` 与 `\`，而 `IDENT_PART` 不认——
  只要一行里出现 `@`（Python 装饰器 `@functools.wraps`、Java 注解、JSDoc 的 `@param`、
  邮箱地址…）或 `\`（Windows 路径），内层 `while` 的游标就**停在原地**，
  外层 `while` 于是无限自旋。它只在「那块代码**第一次进入视口、首次分词**」时才触发，
  所以表现为「滚动到一半卡死」，且与文档大小无关（纯内容依赖）。
- **修法**：
  1. 标识符分支在游标没推进时**退化成「吃掉这一个字符」**——保证推进，也保证拼回原文；
  2. 两个扫描器（`code` / `data`）各加一道**推进保证保险丝**：任何分支让 `i` 原地打转就强制前进一格。
     这类「匹配了却不推进」的写法以后不可能再冻死界面。
- **验证**：**14 种语言 × 17 种恶劣输入 = 238 个用例零卡死、全部逐字节还原**；
  同一份文档（此前二分出 2 个毒块）现在 20/20 全通过；自检 +14 条回归断言（**1884 项全绿**）。

### 修复 · 探针在「永久死循环」这种最坏情况下也要能留证据

- **阶段一变就立刻落一行**（相邻重复去重）：同步死循环不让出主线程，心跳会停、看门狗也没机会说话，
  这行「进入致命阶段的时间戳」是唯一能拿到的现场（配合主进程 `unresponsive` 时刻即可算出跑了多久）。
- **回合结束后诊断再保持 120 秒**：实测冻结发生在「AI 生成完 → 用户开始滚动」那一段，
  以前回合结束就关掉诊断，白丢现场。
- **常驻「主线程停顿」看门狗**：一恢复就写 `主线程停顿 XXXXms（停顿前最后阶段：…）`，
  给出以前最缺的**时长**；打包版、诊断未开也有效。
- 修掉现场转储的**异步拒绝未捕获**（`diagDump` 失败会在控制台刷 `Uncaught (in promise)`）。
- **看门狗区分「后台节流」与「真停顿」**：窗口被遮挡 / 最小化时 Chromium 会把定时器压到 1Hz，
  那是节流不是卡顿——之前空闲窗口里整屏都是「主线程停顿 1000ms」，把真现场淹了。
  现在页面不可见不报、连续多拍都慢（基线本身就慢）不报，
  只有「单独一拍慢」或「远比当前节奏更慢（≥3 倍）」才算真的堵住主线程。

### 新增 · 卡死取证升级（为「AI 一次性补全 10–15 个代码块就卡死」这条复现埋点）

- **探针从「每秒心跳」升级为「阶段进出一对 + 耗时 + 总量」**（`dev/stage.ts`）：
  - `进入 X` 在每个阶段**进入时立刻落一行**——卡住的阶段永远不会自己报完成，
    所以**日志最后一条就是案发现场**；
  - 单次超过 120ms 立刻记 `慢 X …ms`（卡顿的构成单元一眼可见）；
  - 每秒心跳带计数与总量（如 `代码块渲染×288`、`代码块 span 共 45 万`）——
    这正是「写入次数 × 代码总量」二次放大的直接证据；
  - AI 回合结束打一行**耗时归属**：`画布布局×12 共 3400ms(最慢 1400) · 应用写意图×12 共 210ms …`。
- **关键判别点**：`进入 画布布局` 与 `画布提交完成` 两头都打点——
  最后一条落在前者＝卡在**计算**，落在后者之后＝卡在 **DOM 提交**（token span 爆炸）。
- **打包版也能取证**：`[stage]` 通道本来就由主进程落进应用日志；这次顺带修掉一个隐患——
  主进程的 `console-message` 处理器只认旧签名，Electron 32+ 换成事件对象签名后可能**静默失效**，
  现在两种签名都兼容（这条通道是渲染进程被堵死时**唯一可靠的出口**）。
- **开关纪律**：日志只在开发版或 AI 回合进行中才写；日常编辑零开销。转储自带
  「序列化超过 25ms 就退回节流」的自适应保护。
- 新增 [`docs/diag-freeze.md`](docs/diag-freeze.md)：现场文件位置、探针样例、
  **判读表**（最后一条日志 → 结论 → 下一步）、离线二分复现流程。

### 布局收尾 · 偏移避让补齐 + 边界/概要预留空间 + 两处布局热点

- **手动偏移避让补齐到全部家族**（文档里「其余家族留待后续」那条）：
  组织架构行做**横向右推**、矩阵做**同列下推**；括号图与树状表格本来就复用共享堆叠路径，
  自动继承。顺带修掉一个真缺陷：**顶层矩阵以前完全不认手动偏移**——它用的是自己那套 walk，
  既不吃 `position`，也不会有避让（拖动等于没反应）。矩阵现在纵向偏移生效、
  横向钳在列内（表格语义下不该挪到隔壁列上）。
- **概要/边界参与布局空间预留**（Xmind 同款行为）：把「边界/概要向外占用的量」
  交给布局，与绘制共用同一批常量，所以「留出来的白」与「画出来的黑」不会各说各话。
  竖直堆叠家族（思维导图各向 / 逻辑图 / 树形图 / 树状表格 / 括号图）按上/下留白，
  组织架构行按整行留白 + 给右邻居留出括号宽度。**未覆盖**：鱼骨图、时间轴、放射图、
  矩阵行距（覆盖层朝向各不相同，留待逐结构验证后接）。
- **布局热点两处**（审计发现）：
  - `addOverlays` 以前**每个**边界/概要都各自递归一遍自己区间的子树（B 个边界 = O(B×节点数)），
    现在一次后序遍历建好「子树包围盒」索引，取任一区间只花 O(区间长度)；
    边界还顺手去掉了「同一个 range 解两遍」的重复解析。
  - `maxDepth`（鱼骨/时间轴用来定主脊长度）以前**无缓存**，深嵌套且逐层声明同类结构时
    退化成 O(节点数 × 深度)；现在按 id 缓存。
- **不做的**：真正的「仅子树局部重算（增量布局）」——`known-issues.md` 已论证
  连线、坐标归一化与按层/行/列的全局聚合本质上都是全局的，需要一次独立重构，
  不在本批内。
- 自检新增 **10 条断言**（偏移避让 5 条 + 边界/概要预留 5 条），
  且都做过「关掉修复就变红」的灵敏度验证：**1870 项全绿**。

### 优化 · 工具层「每次写入都放大一次」的成本（顺「加了工具之后才冻结」这条线索排查）

- **高亮缓存有个病理口子**：单个代码块超过 20000 字符就**不进缓存**——AI 往一个节点里写
  整份文件后，此后每一次重排（每次 store 改动、每敲一个字）都会把这段大代码**全量重新分词**，
  正是「内容依赖型」的形状。改成按**总字符预算**限流（64 条 / 4M 字符）：
  大块也能命中，内存仍有上界。
- **批量移动 O(k×N) → O(N+k)**：`moveTopics` 以前逐条调 `moveNode`，而每条都要跑一次
  `settleAfterMove` → `pruneOverlays` 的**全树扫描**——一次最多 200 条就是 200 遍全树，
  是「AI 批量整理大导图」时的固定放大器。新增 store 批量入口 `moveNodes`：
  一次写入落完、覆盖层只在末尾清一次，终态与逐条调用完全一致（自检补了等价性断言）。
- **折叠徽标计数曾经每个节点都要算一遍整棵子树**（O(节点数 × 深度)）：`countDescendants`
  加身份缓存——immer 下改过的子树必然换引用，未改动的直接命中。
- **代码块是「一个 token 一个带内联样式的 `<span>`」**，而 AI 每写一次都会让布局对象换新、
  代码节点必然重渲染——不缓存就是百万级元素重建。现在把元素数组按（内容, 语言）缓存：
  内容没变时 React 直接跳过整棵子树。实测单块 2KB / 10KB / 50KB 分别是
  414 / 1918 / 9438 个 span；24 个代码块 × 60 次写入下，未缓存时的重建次数是
  59.6 万 / 276 万 / **1359 万**次——这就是「24 主题的小文档也能冻住」的原因：
  决定成本的是**内容量**，不是节点数。
- **取证转储的自我保护**：强制转储在大文档下每次写都要同步序列化整份 workbook，
  实测单次超过 25ms 就退回节流模式——取证工具不能自己变成新的卡顿来源。

### 新增 · 冻结取证工具就位（自取证转储 + 内容二分器）

- **自取证转储**：AI 回合期间把现场（消息线含全部工具参数 + workbook + 阶段）**节流 2 秒**
  写到 `%APPDATA%/smind/diag/last-state.json`，每次写意图落地后**强制**落一份。
  历次冻结都发生在「写意图落盘后的渲染」，而未命名文档没有自动存档、聊天历史也不落盘——
  强杀进程会把毒内容一起带走，下一轮只能从零猜；现在冻结后现场一定还在磁盘上。
- **内容二分器**（`npm run diag:freeze [xmind 路径]`）：对文档里的**每个代码块**跑渲染同一套
  流水线（分词 / 代码块指标 / 标题寻址匹配），每块一个独立进程 + 4 秒超时——
  哪个块不返回，哪个块就是毒块，并把内容写到 `.tmp-check/poison-N.txt`。
- 移植时修正两处实跑即崩的问题（工具此前从未被真正验证过）：
  `content.json` 的结构假设写成了 `.sheet.rootTopic`（实际是画布数组、元素直接带 `rootTopic`）；
  自动存档路径写死了用户名，改为按 `APPDATA` 推导。
- 已用构造样本端到端验证：2 个代码块逐块通过。
- **自测钩子（补上此前唯一没被真正执行过的分支）**：`DIAG_FAKE_SPIN=<索引>` 让指定块
  **永不返回**，用来把「超时判毒块」这条**判决路径**真的跑一遍——真触发过它的自旋 bug
  已经修掉，这条路径长期处于"逻辑上应该对、从没被跑过"的状态。
  实测：`#1 OK / #2 ★★★ 毒块`，内容正确落盘到 `.tmp-check/poison-2.txt`。

### 修复 · 随回退丢失、按提交逐个挑回

- **折叠徽章的数字被截成「2…」**（视觉 bug，AI 批量补全后必现）：徽章绝对定位在节点右缘
  （`left: 100% - 8px`），收缩宽度只剩 8px 可用——折叠数一到三位数就被截断。
  显式 `width: max-content` + `white-space: nowrap` 让它按内容撑开；悬停提示补上精确数字。
- **状态栏常驻「视角锁定」开关**：以前只在开启时才出现——关着就隐形，用户找不到入口。
  现在常驻显示，关闭态灰显但仍可点（标题里写明快捷键）。
- **视角锁定开关跨会话记忆**：`viewLock` 原来是纯会话状态，每次重启 / 新开文档都回到
  设置默认值——用户刚打开开关，窗口一重启就"消失"。现在用 localStorage 记住最近一次选择，
  启动 / 新建 / 打开文档都恢复它；「启动默认视角锁定」设置只在从未动过开关时作为初值。

### 优化 · AI 批量写入后的卡顿与模型往返（内容依赖型卡顿的两条主线）

- **代码块高亮加了渲染缓存**：以前每次全页重排（AI 批量写入、任何无关节点改动）都会把
  每个代码节点里的整段代码**重新逐字符分词一遍**——纯 CPU、零缓存，代码越多卡得越久，
  与「内容依赖、分钟级」的冻结症状吻合。现在分词结果按 `(语言, 内容)` 复用
  （固定容量 64、超大代码不入缓存），分词结果是只读数据，行为完全不变。
- **已执行工具调用的参数压缩成短摘要**：轮内每执行完一个工具调用就把**整条消息线**
  原样重发给模型——`setCode` 的整段代码、`insertSubtree` 的整段大纲全都原样带着，
  几十个调用就能把单轮提示词撑到 60k+ token，光让服务端读一遍就要一两分钟
  （日志实测单轮往返 125 秒：UI 没死，但像死了）。
  现在成功执行的调用只保留开头 200 字符加省略说明（执行结果本来就在对应的工具消息里）；
  失败/未执行的调用不压缩，保留模型自我纠错所需的完整参数。
- 顺带：代码块结构的类型统一到共享的 `TopicCode`，消除三份手写内联定义。

### 修复 · 弹窗与粘贴（AI 设置 / 发送报错）

- **鼠标划出弹窗就把设置框关掉**：遮罩原来只判 `click`——用户按在弹窗内部、把鼠标划到外面
  才松开时，浏览器仍会往共同祖先（遮罩）冒泡一个 `click`，于是被当成「点了空白处」。
  现在要求**按下与松开都落在遮罩上**才算点了遮罩（对所有弹窗生效）。
- **保存后自动关闭**：AI 设置保存成功即关掉，不再要求用户再点一次「关闭」
  （「测试连接」里那次隐式保存不关，避免测试完就被弹出去）。
- **粘贴长内容后发送失败只报「对话内容无效」**：额度按真实使用放宽
  （单条 6 万字 → **20 万字**，条数 120 → **400**），并且报错说清是哪种超限、该怎么办；
  同时把 Electron 的 `Error invoking remote method …` 包装剥掉，只留人话。
- **剪贴板里是图片时粘贴静悄悄没反应**：现在会明确提示「聊天目前只能发文字」，免得被当成功能坏了。

### 新增 · 二期第二批写工具：AI 能碰「导图的另一半」

- **8 个新写工具**：连关系线、加边界、加概要、改这三者上的文字、删掉它们、设置标记图标、加/去标签。
  在此之前 AI 只能改**主题树**——关系线、边界、概要这些"表达关系与强调"的部分它完全碰不了。
- **新增只读工具 `listAttachments`**：列出画布元素（含 id）与**可用标记 id 清单**。
  这些元素没有标题可寻址，模型必须先用它拿到 id；标记清单不查，模型就会自己编一个不存在的 id。
- **语义按「模型安全」设计**：现成的 `addRelationship` / `addBoundary` / `addSummary` 读的是
  **用户当前选中**（AI 一改选中就搅乱用户选区），而且是「再点一次就删」的开关语义
  （模型重试一次就把线删了）→ 新增 4 个**不依赖选中、幂等**的 store 动作：
  `connectTopics` / `addBoundaryFor` / `addSummaryFor` / `setMarkers`。
- 边界/概要要求**同级相邻**才圈得出范围；圈不出来时如实报错并回喂给模型自己纠正。
- 删元素仍是**破坏性操作**：照旧先问用户一次。
- 写工具合计 **18 个**（第一批 10 + 第二批 8，守住「单批 ≤ 12」的纪律；数量用等值断言钉死）。
- 验证：自检 **1847 项全绿**（+46：意图解析、破坏性标记、标记 id 校验、缺 id 时的引导、
  闸门关闭时不下发、工具数量钉死，以及映射到的 4 个 store 动作本身——**幂等**、
  **整体替换**、空列表圈不出范围、都进撤销栈）。

### 新增 · 0.9.0-beta.1：许可激活入口（可分享给他人试用了）

- **常驻的激活入口**：以前只有"试用用完"之后，聊天面板里才露出许可码输入框——
  想**提前支持**的人在试用期里根本找不到地方激活（真被问过）。
  现在两处都能进：
  - **聊天面板的「试用剩 N 次 / Pro」徽标**本身可点 → 直接跳到 AI 设置；
  - **AI 设置里新增「许可（Pro）」一节**：常驻显示状态（试用剩几次 / 已激活（持有人））、
    粘贴许可码即可激活、已激活还能「取消激活」（换机器 / 退货都用它）。
- 许可仍然是**纯离线**验签（Ed25519），不发任何网络请求。
- 版本号 `0.8.0` → **`0.9.0-beta.1`**：`v0.8.0` 已经发布过，分享包不能与它同名不同内容。
- 打包产物（`npm run dist`）：`release/SMind-0.9.0-beta.1-x64-setup.exe`（安装版）、
  `release/SMind-0.9.0-beta.1-x64-portable.exe`（免安装，分享首选）。

### 新增 · 卡死的「黑匣子」：阶段心跳

- **为什么需要它**：同步死循环会**彻底堵死**渲染进程主线程——调试端口进不去、
  V8 采样要等进程正常退出才落盘（而卡死时只能强杀，等于什么都没留下）。
  于是加一条**在堵死状态下仍然有效**的线索：每秒把「当前在哪一步」写进控制台，
  主进程把它落进应用日志。**卡死时日志里最后一条心跳就是案发现场**。
- 已埋点：应用挂载完成 / 画布布局（前后）/ 镜头跟随 / 字体就绪后重算 / AI 第 N 轮 / 执行工具 X。
- 只在开发版运行（打包版不写日志、不跑定时器）。

### 修复 · 卡死不再需要强杀进程（10 秒自动恢复）

- **现象**：界面卡住后窗口连「关闭」都点不动，**调试命令也永远不返回**——说明主线程被
  **同步死循环**占住，而不是普通的慢（普通的慢每帧都会让出，调试命令进得去）。
  用户除了强杀进程没有别的办法。
- **对策（主进程自救）**：界面无响应超过 **10 秒**就自动重新加载界面，重载后弹一条说明。
  自救必须放在**主进程**——渲染进程被堵死时只有主进程还活着，这条路线一定走得通。
  文档每 30 秒有一份自动存档兜底；若中间自己缓过来则撤掉自救，不打扰。
- **黑匣子**：开发版可用 `SMIND_PROFILE=1` 启动，给渲染进程开 V8 采样；下次卡死会在项目根目录
  留下 `isolate-*.log`，`node --prof-process` 就能看到**卡在哪个函数**。
  （卡死时远程调试端口进不去，这是唯一能拿到调用栈的办法。）
- 另修：画布容器的 `ResizeObserver` 回调改为「尺寸真的变了才更新状态」，
  避免尺寸抖动被放大成自激重渲染。

### 修复 · 渲染进程烧满 CPU（「点击任何键都没反应、关也关不掉」）

- **现场**：应用日志里连续 `unresponsive | 渲染进程无响应`，任务管理器里**渲染进程烧掉几十秒 CPU**
  （其它进程只有 1 秒上下）——不是 AI 在干活，是界面里有个循环停不下来。
- **重点嫌疑**：「视角锁定」的跟随循环。它是「每帧向目标逼近 22%」，只有走到亚像素
  （`< 0.5px`）才收手；**只要有一处不收敛**（几何每帧都在变、视口尺寸在震荡、pan/zoom 变成 NaN），
  这个判断就永远为假 → 永久烧 CPU。而 `defaultViewLock` 默认是开的，所以从启动就在跑，
  重启也躲不掉。
- **修法（双保险）**：① 帧数止损——240 帧内不收敛就停手，并把现场（`focusKey`、`pan`）写进日志；
  ② 依赖每秒重跑超过 60 次（说明有东西在震荡，跟随会自我驱动成死循环）就暂停跟随。
  宁可不自动跟随，也不能把窗口卡死。
- **卡死时的出路交给主进程**：连续两次「无响应」会弹一个**原生对话框**，可以直接「重新加载界面」——
  渲染进程卡住时原生对话框照样能点，这是那时唯一还能点的按钮。
- **诊断增强**：渲染层的 `console.warn/error` 现在会写进应用日志，下次翻日志能直接看到是哪个模块在刷。

### 修复 · 卡死（点击任何键都没反应、关也关不掉）

- **根因**：工具队列是**同步递归**跑的（`queue.index += 1; step()`）——模型一次批量发几十个
  工具调用时，全部画布写入 + 重排会在一次调用栈里跑完，渲染进程主线程几分钟不回。
  应用日志里留下的就是连续的 `unresponsive | 渲染进程无响应`。
- **修法**：每个调用之间**让出一帧**（`setTimeout`），界面能持续重绘，Esc 与「停止」也真正生效；
  状态行顺便显示进度（`正在改画布…（12/40）`）。
- **「停止」现在真的能停**：以前只取消了网络请求，已排队的工具调用还会继续改画布——
  用户按了停止、画布却还在变，比不给停更让人生气。现在停止会清空队列并把回合收干净（撤销照样一下回退）。
- ⚠️ 卡死时窗口无法响应任何按键（这是主线程被占住的必然结果），只能强杀：
  `taskkill /F /IM electron.exe`。

### 修复 · `对话内容无效` 的真凶（批量调用被误判）

- **症状**：批量整理大文档时突然弹出 `Error invoking remote method 'ai:chat-stream': Error: 对话内容无效`，
  整回合直接死掉，重发同样的指令还会再犯。
- **根因**：主进程的入参校验里写着「单条消息最多 20 个工具调用」——而大文档批量删除/改名时，
  模型一次回复发几十个调用很自然（提示词里还鼓励它批量发）。撞上就被判成「内容无效」。
- **修法**：额度改成**只拦垃圾**的量级（单条消息工具调用 20 → 200、正文 6 万 → 100 万字），
  并且所有「对话内容无效」都改成**能定位**的报错（第几条、role、content 类型、toolCalls 个数、哪个字段不对）。

### 新增 · 开发版状态栏显示「构建 HH:MM:SS」

只为回答一个问题：**眼前这个窗口是不是旧的**。开发期最气人的情况是改了代码、窗口还停在两小时前
那份，用户在旧界面上复验、白折腾一轮（今天就踩了一次）。构建时间由 `electron.vite.config.ts` 注入，
只对开发版显示。

### 修复 · AI 聊天「看不出它在干活」

- **在聊天里选中文字按 Ctrl+C 复制不出来**：全局快捷键无条件 `preventDefault`，把 Ctrl+C
  一律当成「复制画布选中的节点」，于是面板里的文字永远复制不下来——用户报的
  「**无法从会话粘贴东西**」根因就在这儿（复制不出，当然粘不了）。
  现在页面上**真的有文字选区**时一律交还浏览器（画布节点不可选中，所以有选区必然是面板里的字）。
- **分不清它是还在想、还是已经答完了**：新增「正在工作」指示——转圈 + 一行说明
  （`正在思考…` / `正在翻看导图…` / `正在改画布…`，多轮时标出第几轮），
  流式回答末尾还会带一个**闪烁光标**，一眼看出这条还没写完。

### 修复 · 画布体验（折叠视角 / 关系线换行）

- **折叠 / 展开不再把视角带走**：点折叠时，被点的那个主题**留在屏幕原处**（反解平移量），
  画面不再整体跳走。更关键的一处：选中的主题若正在被折叠的那一支里，它已经从布局里消失了，
  视角锁定再也盯不到它——用户看到的就是「锁定突然失效、画面不跟了」；现在会把选择
  **提到折叠节点自己身上**（既看得见，锁定也能继续跟）。
- **关系线 / 边界标题支持手动换行**：以前只有概要能换行，这两种是单行 `input` 编辑 +
  单行 `<text>` 渲染，用户根本没处按回车。现在三者统一走 textarea
  （**Enter 换行、Esc 取消、Ctrl/Cmd+Enter 提交**、失焦也提交），渲染按 `\n` 拆行并整体垂直居中，
  尺寸沿用已有的多行估算；右侧属性面板里的关系线 / 边界输入框也一并打开多行。

### 修复 · AI 聊天面板（粘贴 / 消耗显示 / 停止标记）

- **便捷粘贴**：输入框旁新增「粘贴」按钮——走主进程读剪贴板（比渲染层 API 稳，无焦点/权限也不抛），
  内容插到光标处、保留换行、粘贴后光标落在内容末尾。输入框里的 Ctrl+V 原生路径不受影响。
  注意：**菜单「编辑 → 粘贴主题（Ctrl+V）」是画布命令**，粘的是主题节点不是文字——
  在聊天输入框里粘贴请用 Ctrl+V 或这个新按钮。
- **token 消耗显示**：每次回答底部显示本次消耗（问 / 答分列，多轮工具调用**累计**）；
  面板头部显示会话累计，清空对话时归零。数字一律按**服务商回报**统计，服务商不支持就不显示——
  **不做估算**（估出来的数字是编的）。实现：请求带 `stream_options.include_usage`；
  流末那个 `choices` 为空、只有 usage 的分片必须收下（以前会被当垃圾丢掉）。
- 「（已停止）」不再重复显示两次（内容为空时气泡标记与文案各写了一遍）。
- **试用徽标改为「试用剩 N 次」**：原来的「N / M」写法太像次数上限，用户会误读成
  工具调用限额（真被误读过了）；商业化那一侧的写回合上限照旧由 `TRIAL_TURN_LIMIT` 派生。

### 新增 · 商业化基建（Pro 闸门 / 离线许可 / 30 回合试用）

- **Pro 闸门放在主进程**：由主进程决定**下发哪些工具定义**——免费用户的对话里
  **一个写工具都不存在**，模型物理上调不动它（不是靠提示词拦，也不是渲染层判断；
  这也是提示词注入的最终防线）。
- **离线许可**：Ed25519 签名 + 内嵌公钥本地验签，**不联网、不做在线激活**（符合本地优先）。
  许可码形如 `SMIND1.<内容>.<签名>`，粘进「AI 面板 → 输入许可码」即生效；
  粘贴时带上的换行、空格、全角句点/横线都会自动清掉（不然用户只会看到"签名对不上"）。
- **30 个写回合试用**：只读聊天永久免费且**不计数**——只有真的动了画布的对话才算一个回合，
  计数在主进程（渲染层只负责显示）。用尽后面板显示剩余 `0/30` 与输入许可码入口；
  **只读聊天、以及 0.7/0.8 已交付的一切（含润色/展开/生成三个对话框）一概不受影响**
  ——这是 README 承诺的另一半边界。
- 提示词按许可状态如实切换：不能写时明确告诉模型「写工具没有下发」，并禁止它假装已经改了。
- **自动更新（W3，开发侧的最后一件事）**：打包版启动 45 秒后后台检查更新，**下载在后台、
  安装放在退出时**——不打断正在画图的人；「帮助 → 检查更新…」主动检查并把结果说清楚（包括失败原因）。
  更新渠道（GitHub Releases 的 owner/repo）与代码签名（Azure Trusted Signing）的配置位已留在
  `electron-builder.yml` 并写明步骤：**填上即生效，两者互不阻塞**（签名管 SmartScreen 信誉，
  自动更新只管拿到新版本）。开发版不检查更新。
- **签发工具**（只给项目所有者）：`npm run license:keygen` 生成密钥对、
  `license:issue -- --to 名字 [--order 单号]` 签许可码、`license:selftest` 检查
  「私钥与客户端内嵌公钥是否配套」（专防换了密钥忘粘公钥）。
  私钥写在**仓库之外**（`%USERPROFILE%\SMind-keys\license-private.pem`，可用 `SMIND_KEY_DIR` 改），
  请离线备份；仓库已忽略该文件名做第二道保险。
- 自检新增 **51 项断言**：许可码格式与清洗、**真签真验的 Ed25519 往返**（改内容、换公钥都必须验不过）、
  试用算术（不越界）、写回合判定、状态视图、工具闸门（不能写时一个写工具都不下发）。

### 新增 · 让 AI 动手改图（三期二期，进行中）

- **不再要求用户「先去画布上选中」**：实测模型会把「目标不明确」直接推给用户
  （「请先在画布上选中要细化的节点」），而那个节点标题其实就在**它自己上一条回复里**。
  三处修：① 系统提示词明确「**绝不要反过来要求用户去画布点选**」——标题用 `searchNodes`
  自己找；用户说「改 / 继续 / 动手」这类**复述式指令**时，指的就是你上一条回复里提到的主题；
  ② 应用把**用户这句话里提到的节点**（按标题自动匹配，带句柄与路径）直接注入提示词，
  模型连搜都不用搜；③ 工具结果就地提醒用句柄。
- **「说要改、其实没落地」的三处兜底**：① 写操作**失败也留痕迹**（面板显示「未执行：原因」），
  以前只有成功才有条目，用户看不出哪步没做；② **零写操作但话里带「已改好 / 已完成」这类结果声明**
  时，气泡上如实标注「本轮没有任何改动落到画布上」；③ **空操作不再报成「已执行」**
  （改同名、把已在末尾的节点再移到末尾 → 如实说「没有改动」；同一父级下的**重排仍然放行**）。
  提示词同步加规则：只有工具真的返回「已执行」之后，才可以说「已经改好了」。

- **针对「一次回复只发一个工具调用」的模型做适配**（qwen-plus 这类，用户实测「走一步推一步」）：
  ① 请求带 `parallel_tool_calls: true`，明确允许并行调用；
  ② 系统提示词加规则：一次回复可以带多个调用、同一批操作用**一条** `moveTopics` 发完、
  两次调用之间不写解说文字；
  ③ **工具结果里就地提醒**——模型单发 `moveTopic` / `renameTopic` 时，回喂里加一句
  「剩下的同类操作用 moveTopics 一次批量发出来」；在它当下的决策点纠正，比在提示词里讲一遍有效得多；
  ④ 轮数上限 8 → **16**、调用上限 50 → **60**（单发模型需要更多轮；安全阀仍在，只是不再误伤）。
- AI 设置补上选模型建议：改画布优先用支持工具调用的强模型；8k 级上下文装不下 agent 回合。

- **防「照抄已有内容」把画布复制一份**：整理 / 归类时模型很容易改用 `insertSubtree`
  把已有节点「重写一遍」——那是**新增**，等于复制（实测出过事故：整理后画布上出现了好几套
  相同的分类与节点）。现在两道防线：**硬拦**（outline 里 ≥5 个标题且过半数在文档中已存在时
  直接拒绝，并指引改用 `moveTopics`；确实要新增同名内容须带 `allowDuplicate: true`）+
  **软约束**（工具描述与系统提示词都写明「已有内容一律用 moveTopics 移动，不要重写」）。

- **读工具给模型发「句柄」，大导图整理可以**一回合完成**：大导图里同名标题能出现几十次
  （「创建 socket.socket()」这类），还有超长标题、带斜杠的标题——按标题寻址**不可能唯一**，
  模型只能反复试探（实测 256 节点整理搬了 69 个就卡住）。
  现在 `getSubtree` / `searchNodes` 的每行都带 `[#xxxxxx]` 短句柄，`address` 可直接填句柄：
  重名 / 超长 / 斜杠一律不再是障碍。系统提示词同时写明整理的标准三步
  （一次读到位拿句柄 → 建分类 → 按句柄批量搬完），并明确「不要回头再问用户用哪种方式」。

- **标题里带斜杠的节点不再是 AI 的盲区**：编程笔记里「class A: /A ()」这类标题很常见，
  以前路径解析一失败就直接报错、永远走不到「整串标题精确匹配」这一步——这类节点对 AI
  **完全不可见**（改名、移动全部失败；python 笔记整理实测卡在 3 个带斜杠的标题上，
  模型绕了好几轮都没绕过去）。现在路径走不通会先落回整串标题精确匹配（唯一才接受，
  重名仍然问清），救不回来才报路径错误。

- **「继续」不再失忆**：对话历史里只有模型说的**文字**、没有它做过什么——用户说「继续」时
  模型会从零开始重新读取、重新规划，大导图上就是同一种折腾反复来（实测 100 节点整理体验差的主因）。
  现在把上一轮的实际改动（工具摘要）注入 system prompt，并明确禁止重读重做。
- **整理任务写进行为规则**：直接动手（`insertSubtree` 建分类 + `moveTopics` 批量搬），
  不要在回复里贴「建议的大纲」让用户自己抄；做完只留一两句总结。
- **`moveTopics` 改为跳过容错**：100 条里错 1 条就整批退回 = 白烧一整轮；
  现在能执行的执行、失败的精确列出（第几条、什么原因），全都不行才整体报错。

- **批量移动 `moveTopics`（第 10 个写工具）**：整理大导图（上百个平铺节点）靠它——
  一次调用搬运一批节点，**调用次数上限按「调用」算、不按「节点」算**；
  规划阶段**全有或全无**：任何一条 address 解析失败都整批退回、并指出错在第几条（`moves[3]`），
  部分落空也如实回报「成功 x/y」。上限相应放宽到 **8 轮 / 50 次调用**——
  不去掉上限：没有它，模型陷入循环会无限烧用户的钱；有了批量工具，逐个搬本来就不该发生。
- **气泡版式改为「过程在上、结论在下」**：工具调用痕迹在上、AI 的回答压轴——
  以前回答在最上面、被工具条目和上限提示压住，用户找不到「回复」在哪儿。
  撞上限后的总结现在明确要求列出「已完成 / 未完成 / 建议下一步（例如再说一句「继续」）」。

- **改完看得见**：回合结束时，AI 动过的节点会**闪两下**、视口自动带到第一处改动
  （直接操作没有「预览确认」这一环，信任全靠这一眼）。
- **不丢用户正在输入的字**：AI 动手前先把编辑框里未提交的标题按正常流程提交，
  且这次提交在 AI 事务之外——`Ctrl+Z` 先撤 AI 的改动、再撤这次提交，顺序对得上。
- **不搅乱用户摆好的版面**：手动摆过位置的主题（画布上的自由节点）默认**不许 AI 移动**；
  确实需要时模型必须显式带 `allowMoved: true` 再来一次。

- **AI 能直接改画布了**：新增 9 个写工具——改名 / 插入子树 / 删除分支 / 移动 / 折叠 / 备注 / 代码块 / 公式 /
  提问（`askUser`）。工具全部映射到**已有的、经过验证的 store 动作**，AI 与用户快捷键走同一条路。
- **一次命令 = 一步撤销**：整轮 AI 的改动在结束时并成**一步**，`Ctrl+Z` 一下全回来；
  回合进行中用户级的撤销 / 重做被临时锁住（否则从中间抽走历史会让后续步骤错位）。
- **破坏性操作先问人**：删分支这类操作会在面板里停下来等用户点「执行 / 跳过」；
  跳过会**如实回喂**给模型（否则它会以为已经删了，后面的判断全错）。
- **动手前先存档**：第一笔写操作前自动存一份版本快照——撤销栈在内存里，崩溃就没了。
- **改了什么说得清**：气泡里以小标签列出每一步，收尾再给一行摘要
  （「已改动：…（共 N 处，Ctrl+Z 一次全部撤销）」）。
- 自检新增 **48 项断言**：写工具的参数校验与寻址、事务合并规则
  （含「换父移动 + 改名」这个当年坏过数据的组合）、单实例心跳。

### 修复

- **气泡里只剩一个思维链标签**：推理模型把思维链一起塞进正文时，回答会夹带「think 标签」，
  有时甚至只剩一个**闭标签**孤零零挂在气泡里（服务商把开头那段放进了 `reasoning_content`）。
  现在流式层统一过滤思维链——标签被分片切开也认得出来，被切开时先留住尾巴不显示半截标签。
- **「已达上限」重复提示三次、最后什么都没等到**：撞上限后的「不带工具的决胜轮」以前会反复触发
  （模型还要工具 → 再问一次 → 又撞上限……）。现在决胜轮**只允许一次**，模型再要工具就硬停，
  并在气泡里如实说明原因。
- **读取子树失败**：模型把**文档名**也写进路径开头时一律解析不了（真事：`AI 测试Mind/CART树/…`）。
  现在允许跳过开头一到两段；失败时还会把「这一层有哪些子主题」列给模型，让它下一步自己纠正。
- **AI 造出「新主题」垃圾节点**：模型一次给出多行**并列**内容时，大纲解析器会套一个壳节点，
  而写工具把这个壳直接落进了画布 → 画布上凭空多出一个叫「新主题」的节点。
  现在解析器会标记「根是不是壳」，写工具**把壳剥掉**，让那些行成为并列的新主题（工具描述同步改成这个语义）。
- **撞到工具调用上限就不作答**：以前到上限直接收尾，用户拿到的是半截话。现在上限只当安全阀——
  撞上限时**去掉工具再问最后一轮**，逼模型把已经看到的东西讲清楚（并修正了「助手消息带 `tool_calls`
  却没有工具结果」这个会让服务端直接报 400 的协议顺序问题）。
- **重复调用白烧配额**：同一回合里同名同参数的调用不再重复执行，直接告诉模型「问过了，换个策略」；
  上限也从 6 轮 / 20 次放宽到 **8 轮 / 30 次**（读一篇大文档常常要十几次探查）。
- **撤销栈跨重启会清空**（这是内存里的栈，重启就没了）：AI 改动的摘要里现在写清两条恢复路径——
  按一次 `Ctrl+Z` 整体回退；已保存的文档还会提示「动手前的状态已存进版本快照（`Ctrl+H`），重启过也能回到那里」，
  未保存的文档则如实说明没有快照、重启后无从回退。
- **渲染层错误现在会落盘**：以前只打在终端（`console.error`）里，应用一重启就查不到了；
  而且错误边界**接不住**异步 / 事件里抛出的错误（那类问题连提示都没有）。
  现在错误边界与新增的 `window error` / `unhandledrejection` 兜底统一上报主进程，
  写进 `%APPDATA%\SMind\logs\`。
- **被强杀之后再也启动不了**（双击图标毫无反应，也没有任何提示）：上一次异常退出会留下
  残留的单实例锁，之后每次启动都被判成「已有实例在运行」而静默退出。现在加了**心跳**：
  只有确实有活着的实例在跑才退出，心跳过期就清掉残留锁、重试一次。
  （「启动干干净净、然后什么都没了」是排查时最难查的一类现象。）

## [0.8.0] - 2026-09-15

0.8 的主题是**把 AI 请进软件里**：一个能自己翻看导图、用自然语言回答的助手（免费、只读）。

### 新增 · 内置 AI 助手（聊天面板 + 只读探查工具）

- **新增 AI 聊天面板**：右侧抽屉，工具栏「AI → 打开 AI 聊天…」进入，用自然语言聊当前导图。
- **流式输出**：主进程把 SSE 增量逐块转发给渲染层，回答边生成边显示；可随时「停止生成」，
  已生成的部分保留（不丢）。
- **上下文**：每次提问时**重新读取**「骨架摘要（中心主题 + 一级分支 + 各自节点数）+ 当前选中路径 + 文档规模」；
  万级节点也只有几百 token，且提示词把易变内容放在后面，吃满服务商的 prompt 缓存。
- **只读**：本版本不改画布，AI 只给建议——零数据风险；直接改图的能力在二期（写工具 + 事务安全网）。
- 未配置 Key 时面板给引导；**对话记录按文档保留**（存应用数据目录 `chat/`，用文档路径的哈希做文件名；
  **不写进 .xmind**；未保存的文档只在内存里，关掉即弃）。
- **回复里的节点引用可点击**：点一下就在画布上选中并居中（按首字分桶建索引，大文档也不卡；长标题优先，
  不会被短标题截断）。
- **AI 可以自己翻看导图了（1b）**：注册 4 个**只读**工具——`getSelection`、`searchNodes`、`getSubtree`、
  `getDocStats`；模型按需调用、结果回喂后继续作答（一轮最多 6 个来回 / 20 次调用，超限会**明确告知**
  而不是静默停下）。
- **工具调用循环是自己实现的一层**：流式 `tool_calls` 分片累积（参数是逐片拼起来的字符串，
  覆盖式赋值只会拿到半截 JSON）、按 OpenAI 协议转换消息线（`assistant.tool_calls` + `role: tool` +
  `tool_call_id`）、工具失败**不中断对话**而是把可读原因回喂给模型自我纠正；
  模型不支持函数调用时自动降级为纯对话并在面板上说明。
- **寻址由应用负责**：模型只给字符串（id / 标题路径 / 唯一标题），解析成节点是本地代码的事；
  重名标题**拒绝硬选**并列出候选路径——「自信地改错节点」比报错更糟。工具结果带上限截断，防上下文被撑爆。
- 面板里以**工具痕迹**（小标签）显示 AI 翻看过什么，用户看得见它做过的事。
- 输入法回车不误发（`isComposing` 判断）、Enter 发送 / Shift+Enter 换行。

### 修复

- **点「停止生成」后整块界面崩溃**（`Cannot read properties of undefined`）——
  收尾时用展开写法 `{ ...消息, content: undefined }` 把内容清成了 undefined，下一次渲染读它的长度就崩。
  现在补字段只覆盖**真正给了值**的键（`patchLast`），并在节点引用切分处加了非字符串防御：
  坏数据最多让这段不高亮，不会再把界面带崩。（另：这次崩溃被 0.7 加的**错误边界**接住了，
  没有变成白屏——兜底按预期生效。）
- **错误边界下窗口关不掉、且「重新加载界面」点了没反应**——
  两个根因：① 关闭请求只由 `App` 响应，而错误边界恰好把 `App` 卸载了，没人回应，主进程就一直等；
  ② `will-navigate` 里一刀切 `preventDefault()` 把**刷新**也拦了。
  现在：界面损坏时主进程**跳过未保存确认直接允许关闭**（并记日志）、刷新改由**主进程发起**
  （`window.api.reloadWindow()`）；`will-navigate` 只拦「不是回到自身页面」的导航
  —— 拖进来的图片/PDF 仍会被拦下，原有保护不变。

### 自检

- 新增 **91 项断言**：骨架摘要、系统提示词、SSE 分片解析（半截行缓冲 / CRLF / 心跳干扰）、
  节点引用切分（长标题优先）、聊天记录校验（坏条目丢弃 / 条数上限 / 超长截断）、
  工具调用分片累积（跨片拼参数）、请求体线格式、寻址解析（重名不硬选）、4 个只读工具、循环上限。

## [0.7.0] - 2026-09-15

### 安全与健壮性

- **保存改为原子写**：先写同目录临时文件 → `fsync` → `rename` 覆盖。
  此前是就地覆盖，写到一半被打断（磁盘满/崩溃/断电）会把用户原稿截断成坏包。
- **新增错误边界**：渲染期异常不再白屏，给出错误详情、复制按钮与重新加载入口。
- **新增主进程崩溃兜底**：`uncaughtException` / `unhandledRejection` / `render-process-gone`
  都会落日志到 `%APPDATA%\SMind\logs\`，菜单「帮助 → 打开日志目录」可直接打开。
- **自动保存失败不再静默**：会明确提示"自动保存失败，请尽快手动保存"。
- **损坏文件提示**：压缩包损坏时给出中文说明，而不是 JSZip 的英文异常。
- **生产构建注入 CSP**（`script-src 'self'`、禁 `object` / `base` / `form`）。
- **渲染进程开启沙箱**（`sandbox: true`，此前为 false）。
- **IPC 入参校验**：路径合法性、图片 20MB 上限（`shared/ipc-args.ts`）。
- **外部字段逐字段校验**：富文本 run 与代码块不再用 `as` 强转，颜色/字体限定字符范围。
- **公式报错消息转义**后再拼进 HTML 属性。

### 性能

- **测量缓存新增按对象身份的快速路径**：编辑时每敲一个字都会重跑全量布局，
  未改动节点此前仍要逐个重建缓存键，现在直接命中。
- **缓存淘汰改为"淘汰最旧一批"**（此前到上限整体清空，会造成周期性卡顿尖峰）。
- **搜索防抖** 180ms（画布高亮与搜索面板共用）。
- **大纲面板**禁用视口外行的布局与绘制（`content-visibility`）。
- **筛选算法**去掉每层递归都复制路径数组的开销。
- **修复**：改「默认对齐 / 代码块基准字号」后布局不重算的问题。

### 工程

- 接入 **ESLint（flat config）+ Prettier + EditorConfig**，`npm run lint` 零 error / 零 warning
  （脚本带 `--max-warnings 0` 锁住基线）；顺带修掉 hook 依赖导致的闭包读到旧值问题。
- 接入 **CI**（`.github/workflows/ci.yml`）：类型 + 规范 + 自检 + 样本往返。
- tsconfig 增加 `noUnusedLocals` / `noUnusedParameters`。
- 新增 `CHANGELOG.md`、`CONTRIBUTING.md`、`SECURITY.md`、`docs/known-issues.md`。
- 收紧零散实现：外部数据收敛（`shared/model/coerce.ts`）、缓存淘汰（`shared/cache.ts`）、
  原子写（`main/atomic-write.ts`）、编辑态（`NO_EDITING` + `editingContent`）。
- 修复：工具栏「关系线 / 概要 / 边界」三个按钮**丢失了禁用原因提示**（变量算了却没用上）。
- **全仓启用 `noUncheckedIndexedAccess`**（`tsconfig.strict.json`，覆盖 **94 个源文件**），
  修掉约 200 处"可能取到空值"——其中数处是真会崩的：AI 返回内容里没有大纲行时、
  对话框被取消却仍去取第一个文件时、公式尺寸算不出来时、导出图片像素越界时、
  空工作簿取第一张画布时、Markdown 正则分组缺失时。
  修法一律按语义处理（提前退出 / 合理兜底 / 确证才断言），不做机械 `!`。

### 其他

- 应用图标字母由 **M** 改为 **S**（产品名 SMind），重新生成 7 个尺寸的 ico 与各档 PNG。
- README 修正「同时只能开一个窗口」的过期说明（多窗口早已实现），并补上开发相关链接。

## [0.6.0] - 2026-09-14

- 设置分发到子位置（工具栏 / 主题 / 节点 / 渲染默认值各归其位）
- 工具栏「更多 ▾」里的功能可以拿出到快捷栏，也能右键收回
- 代码块默认语言与默认字号可选
- 修复：长连线消失；右键收纳接线补齐

## [0.5.0] - 2026-09-14

- **多文档标签**：底部任务栏式标签页（浏览器式多文件）
- 上下标：紧贴写法（`a^1^` / `a~1~`）生效，格式栏按钮支持光标处切换
- 折叠徽标改为圆形简约样式（保留数字）
- 修复：套用默认主题时若已经是该主题，不再误记为改动（启动即显示 ● 未保存）

## [0.4.1] - 2026-09-14

- 公式不再被截断，公式节点外框有下限
- **手动拉伸节点**（右下角手柄 + 尺寸下限语义 + 往返保真），拉伸时图片等比跟随
- 设置中心：默认视角锁定 / 默认主题 / 默认对齐
- 标记竖排到节点外侧，折叠按钮显示折叠数量
- Markdown 导出保真（与导入器构成往返），布局无重叠全局断言
- 公式与代码块入口上快捷栏

## [0.4.0] - 2026-09-13

- **图片节点**：`Ctrl+V` 贴截图、拖图片文件、清空标题即成纯图节点
- **代码块节点**：多语言、等宽排版、导出时一并绘制
- **Markdown 导入**全格式提取
- 分支级结构补全：矩阵 / 时间轴 / 括号 / 树状表格全部到位
- 修复：分支上切换结构此前不生效（只读中心主题的结构）

## [0.3.0] - 2026-09-13

- **产品名由 Mind 改为 SMind**（作者署名 Wentun）
- 许可证改为 **PolyForm Noncommercial 1.0.0**：自用 / 学习 / 公益组织可自由使用，商用需另行授权

## [0.2.3] - 2026-09-12

- **从文件管理器打开**：双击 `.xmind`（安装版注册关联）、拖到 exe、命令行参数

## [0.2.0] - 2026-09-12

- 视角锁定（镜头跟住选中的主题，逐帧缓动）
- 选中后直接打字即进入编辑；输入法首字母不再被当成普通字符
- 修复：编辑态长文本边打字边换行，不再撑破节点

## [0.1.1] - 2026-09-12

- 打包复打与分发说明

## [0.1.0] - 2026-09-12

首个可用版（P0 + P1）：

- Electron 桌面外壳、原生菜单、文件打开与保存对话框
- 无限画布：平移、缩放（10%–400%）、适应画布、指针锚点缩放
- 节点编辑、多行文本、自动换行与自适应尺寸
- 布局：思维导图（左右平衡）与逻辑图（向左 / 向右，新建默认）
- 拖拽改变父级、拖到空白处自由摆放、恢复自动布局
- **以 `.xmind` 为原生格式**双向兼容，未知字段原样保留
- 基于 Immer patches 的无限撤销重做（上限 200 步）
- 每 30 秒自动保存、崩溃恢复提示、关闭前未保存确认
- 视口裁剪、文本测量缓存、TypeScript 全量类型检查
- `samples/` 下 21 个真实 `.xmind` 样本
