# 更新日志

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)；
每个版本都对应一次**可运行、可验收**的交付（验收步骤见 [`docs/P1-acceptance.md`](docs/P1-acceptance.md)）。
内容依据提交历史整理，未做美化。

## [未发布]

### 修复 · 本轮代码审计（2026-09-18，分批各自过五道门槛）

> 来源：`docs/shared-audit.md`（83 文件 / 18k 行的 `src/shared/**` 审计）+ 应用层实测。
> 每批都跑 typecheck / lint / format:check / selfcheck / verify 并单独提交；自检 2522 → 2625 项。

- **P1 导出装饰失真**（`export/svg.ts`、`export/raster.ts`、`export/drawing.ts`）：SVG 把删除线写成下划线、
  PNG 与位图 PDF **完全没画**下划线与删除线、缺失图片的占位与画布不一致（现统一为虚线框 + 「图片缺失」）、
  标记条分列处有死三元。修法：装饰统一由 `decorationOf` 产出，位图侧补齐两条装饰线与虚线支持，
  `RectOp.dash` 与 `PathOp.dash` 用同一种写法。
- **多窗口关不掉**（`main/index.ts`、`shared/ipc.ts`、`preload/index.ts`）：退出流程中确认后**仍会关掉这个窗口**
  （以前无条件 `return`，窗口留在原地像卡死）；新增 `window:close-cancel` 通道，取消关闭时复位
  `quitRequested` 与 `allowClose`（此前只能置真，于是之后每次关窗都误走退出分支）。通道总数 70，两侧逐条核对无缺失。
- **关 AI 面板后撤销被锁死**（`components/ChatPanel.tsx`、`store/editor.ts`）：面板随抽屉卸载时只取消订阅、
  **没有收尾回合**，于是 `aiTurn` 永远留着，`undo`/`redo` 被静默挡住（Ctrl+Z 完全没反应），画布还因
  `aiTurnActive` 恒真而一直节流。修法：卸载时取消流并 `commitAiTurn`；`newDocument`/`loadDocument`/`restoreDocument`
  一律复位 `aiTurn`；`usePacedWorkbook` 记住"屏幕上那一份"，进入节流的第一帧直接用最新值。
- **P2 IPC 契约与主进程健壮性**：`aiChatStream` 的 `turnId` 补进 preload（此前被窄化掉，许可层拿不到它，
  一条命令会被按轮计费）；附件只生成一个 id；流式请求用完摘掉 `sender` 的 destroyed 监听（工具循环一轮一个，会攒到上限）；
  设置 / 自定义主题 / AI 配置 / 自动保存元信息 / 打开历史索引 / 快照索引**全部改原子写**（`writeJsonAtomic`）；
  快照索引改为「先落索引、再删文件」；导出临时 HTML 与副本文件改用 `randomUUID`（毫秒时间戳会撞名）；
  拖入文件的 `file://` → 路径还原支持 UNC 与 POSIX（`pathFromFileUrl`，UNC 以前被当相对路径丢掉）；
  `openSheetWindow` 失败落日志。
- **P3 重复实现与卫生**：`plan-write` 自造的 `subtreeContains` 并入 `isSelfOrDescendant`（**顺带修掉**"把主题移进自己
  浮动子主题下面"被放行的缺口——store 与拖拽早已拦住，只有 AI 写路径漏了）；`undo` 不再就地改 store 里的历史条目；
  `App.tsx` 死依赖、`NodePanel` 错位 import、`tabs.ts` 硬编码行号等清理；`_audit 结论逐条复核`，
  并记录四处**经核对不成立**的建议（见 `docs/known-issues.md`）。
- **shared 层正确性**（`tree.ts`、`search/index.ts`、`import/markdown.ts`、`outline/index.ts`、`ai/stream.ts`、
  `xmind/xml.ts`、`code/lexer.ts`、`license.ts`、`document/index.ts`）：自由摆放的主题之前**删不掉也移不动**（`allChildrenOf`
  口径统一）；大小写不敏感匹配用字面量正则（`İ` 类字符导致下标错位、替换删错字）；Markdown 开头水平线不再吞掉全文、
  未闭合围栏不再整块丢弃；导出标题按 Markdown 转义（`3*4` 曾被读成斜体）；合并提示词不再缺规格与质量判据；
  搜索/计数/替换统一 `normalizeQuery` 口径；XML 闭标签**对名字弹栈**（错位嵌套不再把节点挂错父级）；
  zip 解压前按**声明的**未压缩大小拦压缩炸弹；SSE 分割器补 `flush()`（末尾不带换行的 `data:` 行会整段丢正文）；
  CSS 字符串认转义（`content: "a\"b"` 不再腰斩串色）；许可 payload 校验日期格式与长度上限；「昨天」按日历日算。

### 文档 · 清理过时与无用文书（只动文档，不碰代码）

- **删除 `docs/structure-specs.md`**：其主参考是那篇被证伪的 CSDN 文章（层级方向做反的根源），
  结论已被官方口径的 `docs/structure-spec.md`（官方原文依据 + 我方取值）全面取代；
  CHANGELOG 里指向它的历史引用同步改为「已被取代并删除」。
- **删除 `docs/project-review.md`**：2026-09-13 的阶段性复查快照（1124 项自检时代），
  文中自述「不属于产品交付物，不需要时可删除」；其全部建议（拆提交 / build 入库 /
  drag-snap 收口 / P1-acceptance 补节等）均已落地。
- **`docs/requirements.md` 标记为历史存档**：立项时「自用 / 内部使用、无需授权」的定位已被
  商业化计划（PolyForm NC 开放 + Pro 买断）取代，P0–P9 计划已全部交付；
  技术选型依据保留存档，现行有效文档在页首指明。
- **`docs/distribution.md` 小勘误**：打包示例路径 `d:/SMind` → `d:/Mind`，分发话术示例版本号改通配。
- 其余逐份复核判定保留：`diag-freeze.md`（活排查手册）、`drag-snap-research.md`（已改写为最终结论）、
  `P1-acceptance.md`（验收总账）、`known-issues.md`（遗留与刻意不做记录，另一会话刚更新过，本轮不动）、
  `phase3-plan.md`、`release-0.8/0.9.md`、`release-mirror.md`、CHANGELOG / README / LICENSE /
  CONTRIBUTING / SECURITY / THIRD-PARTY-NOTICES 均为现行有效。

### 修复 · 边界/概要的空间预留接进全部结构

- **问题**：边界与概要画在区间**外面**，但以前只有「垂直堆叠」与「组织架构」两个家族为它们留白。
  鱼骨图、时间轴（横/竖）、放射图（顺时针）、矩阵图、树状表格这五种结构**完全不预留**——
  给相邻分支加一个边界，边框与标题带就会压在大骨 / 刻目 / 格子上（`known-issues.md` 记的那条遗留）。
- **修法**：
  - 预留从「上/下/右」扩成**四向**，并加 `reserveSpanX` / `reserveSpanY` 供各家族取用；
  - 概要让在**哪一侧**由结构家族决定：多方向结构（平衡图 / 顺时针 / 垂直时间轴）跟**这一支自己的方向**，
    树状表格按「深度＝列」让在右侧，上下生长的组织架构图与矩阵图让在区间下方
    （`overlays.summaryReserveSide`，逐条有断言钉住);
  - 逐个结构把预留接进摆放：鱼骨（大骨偏移按「靠近主脊那一侧的留白」决定、小骨列、向右延伸的列）、
    时间轴（刻目与主轴之间、垂直时间轴的缩进让出概要宽度）、放射图（环间距按"实际占位"算）、
    矩阵（格内堆叠、行高、列宽）、树状表格（行高、表格右边界、网格顶边）；
  - 顺带修掉一处**同源缺陷**：`subtreeExtent` 少算了子节点的预留，于是"子树比槽位高、居中之后顶到下一个
    兄弟"（自检当场抓到：平衡图里 A 支的边界压到 C 支）。
- 自检 **+23 项**：9 条「概要让在哪一侧」的方向断言 + 14 个结构逐一验收「边界/概要不侵入相邻分支」。

### 性能 · 增量布局（`known-issues.md` 里 P2-1 的完整版）

- **问题**：`editingText` 每敲一个键都会变，而布局 `useMemo` 依赖它 → **每击键整图重排**：
  重新测量、重新递归算子树占用、重新拼每一条连线的 path、重新分配每个节点对象。万级文档下就是卡顿。
- **修法**（新增 `shared/layout/incremental.ts`、`stamp.ts`、`run.ts`，`core.ts` 里的 `LayoutBuilder`
  增加一份**跨轮缓存**）：
  - **变更检测是 O(脏路径)**：并行走新旧两棵树，遇到**引用相同**的子树就整棵剪掉
    （zustand + immer 保证未改动的子树引用不变）→ 要看的只有"改动点到根"这一条路径；
  - **子树戳**：每棵子树按「内容 + 尺寸 + 预留」混出一个版本号（`stamp.ts`），戳没变 → 测量结果、
    子树占用、层数、连线 path 全部从缓存里取。**尺寸必须参与戳**：编辑中的节点文字还没提交，
    工作簿里的对象一个字节都没变、宽度却在每个键上变；
  - **零变更短路**：根主题、边界/概要、排版环境三个引用都没动 → 原样返回上一轮那个结果对象（O(1)）；
  - **只换文字就不重排**：编辑中的节点换了文字但**尺寸没变**（还在同一行里改字）时，
    只把它换成一个新节点对象，坐标、连线、装饰线、画布级元素整块复用；
  - **节点对象复用**：主题与坐标都没变的节点沿用上一轮的对象，渲染层的 `TopicNode` 因此能跳过重画；
  - 形状变了（增删 / 换序 / 折叠 / 换结构 / 手摆偏移）走全量，但缓存照旧生效——没碰到的分支仍命中；
  - 全量与增量**走同一段摆放代码**（`run.ts` 的 `runLayout`），差别只在 builder 里带不带缓存。
- 自检 **+73 项**，其中最关键的一条是**「增量结果必须逐个字段等于全量结果」**：节点坐标/尺寸/层数/左右归属/
  换行后的文字、每一条连线的 `d`、每一根装饰线、边界/概要/关系线的几何、`bounds`、分支配色，
  14 个结构各跑一遍；另有一条大文档（303 节点）断言量化"省了多少"：**测量 ≤ 8 次、
  子树占用重算 ≤ 40 次、未受影响的节点整块复用**（都在 3 位数的节点规模下）。

### 新增 · PDF 改成**矢量**导出（超大画布自动回落位图）

- **问题**：导出 PDF 一直是"把画布栅格化后塞进一页 PDF 当图片"——文字不可选中、放大就发虚、
  文件也大（`README.md` 的已知限制与 `known-issues.md` 都记着这条）。
- **以前为什么没做**：矢量 PDF 要自己嵌字体，中文还得做字体子集化，是个独立议题。
  **换一条路就绕开了**：Chromium 自己就是 PDF 渲染器——主进程用一个隐藏窗口加载**同一份导出 SVG**，
  再调 `webContents.printToPDF`，于是文字是真字、图形是真矢量、中文交给系统字体
  （与画布是同一批字体，观感一致）。
- **落地**：新增 IPC `export:svg-to-pdf`（`shared/ipc.ts` 的契约 + preload 暴露 + 主进程实现）。
  两个细节：① 页面尺寸按 CSS px 交给 `@page` 并配套 `preferCSSPageSize`，导出坐标系因此
  1px = 1/96 英寸，**不需要再折算缩放**；② 内容先落临时文件再 `loadFile`——导出的 SVG 里内嵌着
  图片与公式的 data URL，大文档下几百 KB 起步，走 `data:` URL 不稳；窗口用完即毁、临时文件必删。
- **回落（三条判据）**：画布超过 Chromium 的页面上限（约 190 英寸）、打印管线抛错、
  或根本不在 Electron 里跑（浏览器预览 / 自检）。回落时写一行日志，
  **导出永远不会因为"想要矢量"而失败**。
- 这条路自检覆盖不到（要真窗口），所以验收步骤写在 `docs/P1-acceptance.md` 的 `4.47` 节：
  导出 PDF → 在阅读器里**选中文字**、放大到 400% 看边缘是否仍然锐利。

### 修复 · 导出补上「备注 / 链接 / 附件」指示图标

- **问题**（收口 C 类时顺手发现）：节点顶部的指示图标行，画布上有、导出层**完全没有画**；
  更麻烦的是绘制游标也没让出这一行的高度——于是**有备注 / 链接 / 附件的节点在导出图里
  正文偏上、底部空一块**（看不出"少画了图标"，只觉得"文字没对齐"）。
- **修法**：
  - 指示图标行的尺寸与间距收进 `shared/layout/accessory.ts`（测量 / 画布 / 导出三处共用一份，
    各写一份就会出现"测量按两行留了高度、导出只画一行"）；
  - 图标数据由 `npm run marker-art` 一并生成：新增 **notes / link / attachment** 三个图形，
    图标名联合类型 `IconName` 也改成生成（标记图形 + 指示图标）；
  - 导出层按与测量**同一口径**的换行规则（`TEXT_MAX`）算出每一行的位置并在行内居中；
  - `GlyphOp` 增加 `strokeWidth` / `opacity` 两个可选字段——指示图标比标记图标细一档、浅一档
    （与画布 `.topic__indicator` 的 2px / 0.72 一致），SVG 与位图两个后端都支持。
- 自检 **+8 项**：备注与链接各画出一个图标 / 线宽细一档 / 透明度一致 / 三种图标都有数据 /
  **正文被让到图标行下方**（不再重叠）/ SVG 里是真实图形且带透明度。

### 测试 · 补上「标记图标矢量数据」的断言

- `17b3834`（标记图标进导出）的提交信息里写了"自检含『每个图形都有数据』断言"，实际当时没写完，
  现在补上 9 条：内置标记用到的图形都有矢量数据 / 矢量数据覆盖全部图形 / 图形数量与图标表一致 /
  每个图形生成的 SVG 里都有真实图元 / 按 24×24 视图盒缩放并平移到目标位置 /
  线宽与画布上的标记图标一致（不会粗一圈）/ 只描边不填充 / 图形之间不重样 /
  完全没有数据时仍然有兜底。

## [0.9.0] - 2026-09-18

> **本版免费发布，源码开放**——0.9.0 的全部功能都可以免费用；
> 唯一付费点：「AI 直接修改画布」（写工具 agent）为 **Pro 功能**（免费送 30 个写回合）。
> 0.8 及之前的全部功能（0.8 起含 AI 只读聊天）**永久免费、永不收回**。

### 新增 · 统计与骨架会标出「已收起」

- **问题**：`getDocStats` 与骨架摘要报的是**文档里的全部内容**，被折叠收起的节点也算在内——
  模型会把"画布上看不到的分支"当成不存在（或反过来，以为它们正显示着，于是去移动一个看不见的节点）。
- **修法**：新增两个纯函数（`shared/model/tree.ts`）：`hiddenCountOfSide`（某一侧藏了多少，
  徽标与 tooltip 用；`TopicNode` 里那份手写实现随即删掉）与 `countHiddenNodes`
  （整页画布上看不到的节点数，**不重复计入被祖先收起的部分**，用 `visibleChildren` 求差）。
  然后：
  - `getDocStats` 多一行「画布上当前看不到的节点：N（被折叠收起；内容仍在文档里）」，
    **没有折叠时不出现**（正常文档一个字都不多花）；
  - 骨架摘要给被收起的一级分支标上「，已收起」，并在末尾给一行总数提示。
- 自检 **+13 项**（无折叠时为 0 / 按侧收起只算那一侧 / 徽标计数与统计同口径 /
  嵌套折叠不重复计入 / 统计与骨架只在有折叠时才多话），**2409 项全绿**。

### 文档 · README 补「按侧收起」+ 补 `docs/P1-acceptance.md` 的 `4.46` 小节

- **README 使用手册**：新增 `4.4 折叠与「按侧收起」`（折叠怎么用、思维导图左右分别收起、
  圆点贴哪条边、为什么时间轴与鱼骨图只有一个折叠点）；`Ctrl+/` 的注解与「已知限制」里的
  折叠按钮条目同步；AI 一节补上"思维导图还能只收起某一侧"。
  顺带修一处笔误：折叠徽标写的是 **`+N`**，实际只显示**数字**（也没有 `+` 号）。
- **`docs/P1-acceptance.md`**：按项目约定（每个 `4.x` 小节 = 一轮可验收的交付）追加
  **`4.46、按侧收起（思维导图）+ 折叠徽标位置跟随方向`**：做了什么（含范围收窄的理由与
  数据兼容性）、逐条验收步骤、已知限制（组织架构图不做按侧收起的原因）。
- 顺带把「阶段索引」表补到 `4.46`，并把注解里过期的自检数量（2142）更新为当前值（2409）。

### 修复 · 「按侧收起」之后两处入口动作与提示相反

- **大纲那一行 / `Ctrl+/` 显示「展开」，点下去却把两侧都收起来**：
  `outlineRows` 的折叠判据是"有子节点但不是全部可见"，所以按侧收起时那一行显示成「展开」；
  而 `toggleCollapse` 只看 `topic.collapsed`（那时是 undefined），于是按一下变成**整体折叠**。
  现在「按侧收起」也算折叠着：按一下是**展开**（清掉按侧标记），再按一次才是整体折叠。
- **把一级分支拖到已收起的那一侧，分支当场消失**（看着像把数据弄丢）：
  `setTopicSide` 改写左右归属后，那一侧如果正收起着，节点立刻被藏起来。
  现在顺手把那一侧展开——与「新建子主题时自动展开」是同一条规矩。
- 自检 +6 项（`toggleCollapse` 的展开方向与步数、整体折叠不被打搅、
  跨侧拖动自动展开），**2396 项全绿**。

### 修复 · 鱼骨图折叠后留下一截孤线

- **现象**：中心主题折叠（或收起全部方向）后，它右边仍挂着一条**没有任何东西的横线**
  （用户截图）。
- **根因**：主脊的兜底终点写的是「中心主题右缘 + 120」，没有可见分支时**照样画**——
  而这条兜底只在"有分支但还没定位完"时才有意义。
- **修法**：`anchors.length > 0` 才画主脊（与时间轴 `kids.length > 0` 的既有口径一致）。
  没有分支时图里只该剩中心主题。
- 自检 +2 项（折叠后不画主脊、展开后主脊回来），**2390 项全绿**。

### 新增 · AI 也能用「按侧收起」（工具 + 提示词，按三档原则落位）

- **工具（改原有，不加第 32 个）**：`setCollapsed` 增加可选 `side`（`left` / `right`）——
  带 side 就只收起 / 展开思维导图中心主题的那一侧，不传仍是整体折叠（行为不变）。
  只对「思维导图（平衡 / 顺时针）的中心主题」生效：其它主题带 side 会被**如实拒绝并给出下一步**，
  而不是悄悄退化成整体折叠（那会让模型以为"只收了左边"，用户却看到整张图塌了）；
  时间轴 / 鱼骨图刻意不在支持范围内（保持一个折叠点）。
  工具数量不变（写 21 / 读 10），无需改数量断言。
- **新增幂等设置动作 `setFoldSide`**（`setFoldSide(id, side, folded)`）：AI 重试同一设置
  不会把刚收起来的一侧翻回去，也不产生多余的撤销记录（面板点徽标仍走 `toggleFoldSide` 转发）。
- **读取工具能看见收起状态**：`getSubtree` 的行内附注「，已收起左侧 / 已收起左右侧 / 已整体折叠」——
  模型看到的树必须与画布一致，否则它不知道右边为什么只剩一根线，还会重复收起。
- **提示词按三档原则落位**：折叠是**显示操作**，与「生成规模与深度」无关 →
  写进**静态层**【工具与输出协议】（三档一字不差），**不进档位规格**（`qualityRules` 只管生成）。
  `EDITING_HINTS` 补「收起」，让「把左边收起来」注入编辑模块而不是生成规格。
- 自检 **+17 项**（工具的 side 规划与拒绝路径、**时间轴 / 鱼骨图带 side 被拒**、
  顺时针思维导图支持、幂等设置、读取标注、
  **「三档的折叠说明完全相同」与「生成类请求确实注入了档位规格」这两条守门断言**）。
  连同按侧收起与鱼骨主脊修复一起，自检现 **2390 项全绿**。

### 新增 · 思维导图（平衡 / 顺时针）「左右分别收起」

- **诉求**：中心主题两侧都挂着分支时，一根徽标只能"全收/全开"；用户要的是
  「先把左边收起来，右边照旧」。
- **做法**：**思维导图（平衡 / 顺时针）**的中心主题上左右各一根徽标，各贴自己那一侧的边，
  点一下单独收起 / 展开该侧；收起后徽标上显示这一侧藏了多少个节点。
- **范围收在思维导图**：时间轴与鱼骨图的上下两侧只是"沿主轴的交替摆放"，
  把一侧收起来只会让图更难读 → 用户明确要求它们**保持一个折叠点**（走整体折叠）；
  逻辑图 / 树形图 / 括号图 / 树状表格 / 矩阵图 / 组织架构图则是单方向的。
  判据集中在 `splitFoldSidesOf` 一处（只看 `family === 'mindmap'`）。
- **方向抽成唯一来源** `childFoldSides`（`shared/model/tree.ts`）：
  每种结构的布局都从它取方向（脑图交替 / 放射角度半圈 / 时间轴与鱼骨上下交替…），
  收起判定用的是同一份。两条硬性质：**按全部子节点算（与折叠无关）**，
  否则收起一侧后剩下的会重新编号而漂到另一侧；**单方向结构返回空表**。
- **放射（顺时针）**：扇区改为按**全部**一级分支算、只摆放可见的那些，
  否则收起一侧后剩下的会重新均分整圈、铺到收起来的那一侧去（看着像没收）。
  起点角度 `RADIAL_START_ANGLE` 由布局与「左右半圈」判定共用一份。
- **一处口径统一**：`visibleChildren`（折叠加按侧收起）成为布局 / 大纲 / 包围盒
  三处唯一的"可见子主题"来源；以前各自写 `topic.collapsed ? [] : topic.children`，
  加一种折叠方式就得改三处、漏一处就出现"画布收起了、大纲还列着"。
- **两种折叠互斥**：整体折叠（Xmind 的 `branch="folded"`）与按侧收起（私有键
  `com.mindmap.local.fold`，写在 `style.properties`，与 `TOPIC_SIDE_KEY` 同一套做法，
  原样往返、Xmind 忽略）不会同时存在——新建子主题 / 粘贴 / 移动到这里时自动展开。
- 收起某一侧后，落在那侧的**选中主题**会挪到中心主题（与整体折叠同一处理，
  否则视角锁定会盯住一个已经不在布局里的目标）。
- 自检 **+26 项**：模型读写与往返、按侧可见性、布局参与、撤销、互斥、
  `.xmind` 保存/打开，以及**逐结构守门断言**——14 个结构每个都验
  「`childFoldSides` 声明的方向 == 布局真正摆出的方向」
  「收起一侧后其余不漂移」「时间轴 / 鱼骨图与单方向结构保持一个折叠点」。

### 修复 · 折叠徽标位置跟随分支方向（左右 + 上下）

- **现象**：折叠徽标固定挂在节点右侧，与分支的展开方向相反——
  「逻辑图（向左）」的徽标挂在右边（第一张截图）；「组织架构图（向下）」的分支往下长，
  徽标仍挂在右边（第二张截图）。
- **根因**：渲染层只判 `side === 'left'`，其余（含中心主题恒为 `'root'`、以及纵向的
  `up` / `down`）统统落到右侧。
- **修法**：新增纯函数 `collapseBadgeSide`（`shared/layout/core.ts`，进 selfcheck），
  按**可见子节点的最终坐标**判定徽标贴哪条边——全部在下方贴上/贴下（组织架构图、矩阵、
  树状表格、垂直时间轴），全部在左侧贴左（逻辑图/树形图向左），其余贴右
  （平衡思维导图两侧都有分支，不会因为增删分支让徽标来回跳）。
  - 用**几何**而不是子节点的 `side`：`side` 表达的是「节点相对父节点的位置」，不是
    「它的子节点往哪长」——鱼骨图的子节点排在一列却带 `up`/`down`，按 `side` 判会把徽标挂到上方。
  - 中心主题折叠后看不见子节点，退回**结构方向** `StructureDef.grows`（14 个结构逐个声明，
    自检断言"不许漏"），否则「向下的图、徽标挂在右边」。
  - 样式新增 `--up` / `--down` 两个定位（贴在节点上/下缘、水平居中），`--left` / `--right` 不变。
- 自检 **+13 项**（逻辑图左右、树形图向左、平衡图保持右侧、组织架构图上下、折叠后退回结构方向、
  结构方向声明齐全），**2325 项全绿**。

## [0.9.0-beta.2] - 2026-09-16

> 版本号 `package.json` 已同步。`v0.9.0-beta.1` **不补打标签**——它的内容（`e93d003`）
> 与今天的 HEAD 已不是同一份东西，补打会造成「同名不同内容」；本次交付作为 beta.2 单独发布。

### 修复 · 折叠徽标只在子节点超过 1 个时显示；树形图与逻辑图重新区分开

- **折叠徽标**：只有一个子节点时不再显示「−」徽标（用户截图：每个节点都挂着徽标）——
  收起一个再展开一个，图没有任何变化，折叠没有信息量。规则：**子节点 > 1 才显示**；
  已折叠的主题**始终显示**（否则一个子节点的主题一旦折叠就再也展不开了）。
  `store.toggleCollapse` 同口径守卫：键盘（Ctrl + /）与 AI 也折叠不了单子节点的主题，
  免得"看不见徽标、图却少了一截"。
- **树形图 vs 逻辑图**：上一轮把树形图的枝干从直角折线改成了曲线——结果和逻辑图（曲线）
  **一模一样**（用户：「长的一模一样，没有任何区别」）。回退：**逻辑图＝曲线、
  树形图＝直角折线**（官方对两者都只规定了布局方向，连线形状为我方取值，
  但"两个结构必须可区分"是硬要求）。自检新增两条断言把可区分性钉死
  （逻辑图连线全含贝塞尔 `C` 指令、树形图一条都不许有）。

### 变更 · 14 个结构按 Xmind 官方定义逐个对齐（新增 `docs/structure-spec.md`）

用户要求「每一个结构图都按 Xmind 官方的定义来干，不能胡编乱造」。此前修树状表格时
引用了一篇 CSDN 文章末尾标着「**补充说明（非本页内容）**」的段落当依据，把层级方向做反了——
这份教训被固化成了新文档 **`docs/structure-spec.md`**：每个结构的**官方原文出处**
（官方博客 / 官方工具页 / 官方用户指南）、官方确实没写的细节明确标注「**我方取值**」，
以及现状差距与改造记录。

按官方定义逐批落地的改动（每批都跑完整门槛，selfcheck 现 **2309 项**）：

- **树状表格（重做）**：官方说「主题以**嵌套块**的形式显示」「靠**表格的行列结构与缩进/对齐**
  表达层级」「节点默认右对齐」→ 现在是「深度＝列、每个主题一行、同一深度左边缘对齐 +
  **贯穿的网格线**」，没有连线。旧版把"行"理解成"一个一级分支及其全部后代"，
  网格线只能按子树分组画成长短不一的横线（用户：「看着乱七八糟」「根本无法使用」）。
- **矩阵图（重做）**：官方说矩阵是「**二维表脑图**，落点在**行与列的交叉**」→
  现在是「列＝一级主题（第一维）、行＝序号（第二维），同一行**共用基线** + 贯穿网格线」，
  没有连线；纵向偏移不再生效（与"组织架构行"同一套语义），字段保留不丢。
  旧版每列各自纵向堆、再套一个大圆角框——根本没有"行"，读不出二维表。
- **顺时针思维导图**：起点从正上方改为**右上**（约 1 点钟）——从正上方起铺时两个分支
  落在正上/正下，"一上一下"读不出顺时针（用户看着截图问「顺时针是这样的？」）。
- **括号图**：删掉"括号→子节点"的直线——官方括号图页写明层级由「向右扩展的支架（大括号）」
  表达；与矩阵/树状表格同属"靠装饰表达层级"的结构（连线数为 0）。
- **鱼骨图**：同一根大骨的子主题**对齐成一列**，小骨从各自取点水平连到该列——
  旧版子主题沿斜骨错开成阶梯（实测 x=373 vs x=398），小骨只剩 17px 几乎看不见。
- **树形图**：枝干改为**曲线**（官方只规定布局方向，曲线为**我方取值**——
  折线会和组织架构图混同）。
- **时间轴 / 思维导图（平衡）/ 逻辑图 / 组织架构图**：坐标复核确认符合官方形态，维持现状。

### 修复 · 标记图标「同一类别只能有一个」

- **症状**：一个节点上能同时亮着 1 / 3 / 4 / 5 四个优先级（还能同时亮两个星标、两个表情）——
  同一个维度给出四个互相矛盾的答案，既读不出来也用不上；这与 Xmind 的语义也不一致。
- **修法**：
  - 分组表（优先级 / 进度 / 星标 / 旗帜 / 表情 / 符号 / 趋势 / 其他）从渲染层搬进
    `shared/xmind/constants`，**一行就是互斥单位**：点同组的另一个是**替换**，
    再点一次才是取消；不同类别照旧并存（优先级 + 进度 + 星标是三个维度）。
  - 「整体替换」（AI 的 `setMarkers`）也按每行一个收敛；AI 一次给了同组多个时
    **明确拒绝并说明原因**，而不是替它挑一个——悄悄丢掉一个的话，模型会以为两个都设上了，
    下一轮还会这么给。
  - 文件带来的自定义标记不在表里 → **自成一族**，互不影响、也不会被抹掉（导入保真不受影响）。
- 顺带：分组表进了 shared，于是「一行一个」这条规则可以被自检钉住（+16 项断言）。

### 新增 · 框选过程中就能看到「将要选中谁」

- 左键拖框时，被圈到的节点**当场高亮**（与选中同色描边 + 一层外发光表示"还没定"），
  松手才变成正式选中——以前要等松手才知道圈到了哪些。
- 判定与松手落地**共用同一个纯函数**（`topicsInBox`），不会出现"亮了却没选中 / 选中了却没亮"。
  接触边界即算命中（与手感一致），并补 8 项断言（含"框比节点还小"这种边界）。

### 修复 · 拖拽落点「看得懂」与拖拽画面「不乱」

- **提示写出目标名**：空位框下从「将成为子主题」改成「**将成为「中心主题」的子主题**」。
  新子节点永远排在最后一个子节点之后，所以空位框常常离目标很远（放中心主题下面时，
  框会出现在它最后一个分支的下方）——不写名字，用户只能从框的位置反推目标，
  于是必然读成"要成为那个框旁边节点的子主题"。
- **落点高亮由虚线改实线**（3px）：主题本身就可能画成虚线边框（如 forest 主题），
  虚线高亮会和主题边框糊在一起——**等于没有高亮**。
- **自由摆放标记由虚线改细实线**：以前屏幕上到处都是虚线（主题虚框 + 自由摆放虚框 + 落点虚框），
  高亮被淹没。
- **拖拽焦点模式**：拖拽期间只有**被拖子树 + 落点目标（含其父级）**保持原样，
  其余节点淡到 40%、无关连线淡到 30%。只淡不隐，参照还在。
- **被拖节点不再掉队（"拖拽不同步"）**：拖拽期间节点位置改为**直接写 DOM**
  （与 `pointermove` 同帧落地），不再等 React 把整个画布重渲染一遍。以前节点多 / 连线多时，
  重渲染一轮要几十毫秒，快甩一下节点能落后指针几百像素；现在 React 那份状态只喂
  落点预览、高亮与淡化，位移走命令式。
- **修一个能冻住拖拽的隐患**：`pointermove` 的 rAF 合帧原来用"已排队就不再排"防重复，
  而窗口被遮住 / 切后台时 `requestAnimationFrame` 不派发——那一帧没来，
  「已排队」就永远是 true，**后续移动全被跳过**。改为每次移动都重新排一帧。

### 修复 · 顺时针（放射状）：同层半径统一

用户问「顺时针思维导图……这样直接穿线过真的对吗」。探查（打印每个节点的角度与半径）显示
**不是交叉**，而是**同层半径全不一样**：每个节点各自按自己的扇区把半径撑开，
实测同一层的兄弟半径是 **513 / 726 / 965**——画出来就是每个分支射出一根长短悬殊的长线，
看着像"随便穿线"。

- 改成**两遍走**：第一遍只算角度与扇区（与半径无关），
  第二遍**逐层定半径**（本层取该层所有节点的最大需求，且不小于"上一环 + 环间距"），
  **同层同半径**。
- 结果：同层半径统一（965 / 715 / 508…），合成形状（6 分支不等深）与真实文档都 **0 交叉**；
  截图确认放射均匀。

### 修复 · 矩阵图 / 树状表格改成「表格」形状（去掉连线 + 加框）

参考写得很明确：这两种结构**没有连线**，靠**网格线 / 边框与缩进**表达层级。

- **矩阵图**：去掉父子连线（原来的"母线 + 列脊"），改成**每一列一个圆角框**
  （框住表头与它下面的格子）；节点本身就是格子，行列感由框与方块表达。
- **树状表格**：同样去掉连线，改成**每一层一个竖长框**（一层 = 一列表格）。
- 自检不变量随之按**结构家族**调整（不是放松，而是换了判定方式）：
  「每个非根节点要有归属感」——普通结构看"有没有自己的入边"，
  表格类看"结构有没有画出框"；「连线数 = 节点数 - 1」对表格类改为期望 **0**。

### 修复 · 鱼骨图：子主题改成「小骨」（按最新参考）+ 修掉审计自身的假阳性

用户反馈「大结构没问题，**子节点格式**不对」，并要求以最新参考为准。查证结论（当时记录于 `docs/structure-specs.md`；该文件以 CSDN 文章为主参考，后被官方口径的 `docs/structure-spec.md` 取代并删除）：
鱼骨图第二层是**小骨**——从大骨引出、**水平短线、与主骨平行**；第三层起继续向右延伸。

- **鱼骨图重做**（`layoutFishbone`）：
  - 小骨 = 沿大骨按比例取点，从点上**水平**拉一条短线到子主题（与主脊平行）；子主题**以取点为中心**。
  - 大骨跨度按"最多小骨的那一支"自动加长（`(小骨数+1) × (小骨高+行距)`），否则小骨会互相叠。
  - 第三层及更深**向右延伸**（不再顺主脊方向堆，避免撞上隔壁大骨的地盘）。
  - 大骨占位算上小骨子树的向右延伸宽度，否则相邻大骨会互相压。
- **顺带修掉两个真 bug**：
  - **大骨连线混用坐标系**：以前把"归一化之前的 x"和"归一化之后的 y"拼在一起，大骨方向被拉歪、
    从子主题身上压过去。现在统一用最终坐标。
  - **审计自身的假阳性**：`segmentHitsBox` 把线段当**轴对齐**的判（只比包围盒），
    于是鱼骨的**斜线**一律被误报成"穿框"——上一轮"越修越红"就是它害的。
    现在改成真正的**线段-矩形求交**（Liang-Barsky），并给"端点相接"（小骨挂在大骨上）放宽 2% 容差。
- 自检 **2293 项**全绿（9 种形状 × 14 个结构：重叠 / 穿节点 / 线穿线）。

### 修复 · 列脊的左右侧固定 + 自检新增「线穿线」

用户接着报「矩阵图的两边格式不一样」「顺时针随便穿线」「鱼骨也是格式不统一」：

- **脊的左右侧固定**：以前按「这个子节点相对父节点中线在哪边」**逐个**决定 →
  把一个子节点拖偏，同一列里就会出现一半脊在左、一半在右（＝「两边格式不一样」）。
  现在同一父节点下的子节点**永远从左侧进脊**：结构化的形状不该随内容/拖动变。
- **自检新增「线穿线」检查**（`lineCrossingProblems`）：以前只查「线穿节点」，
  放射状那种长斜线可以一根节点都不碰、却横七竖八互相交叉（＝「随便穿线」），**根本查不出来**。
  现在 9 种形状 × 14 个结构都查「线互相穿过」（同一父节点发出 / 指向同一子节点的不算）。

### 修复 · 列式结构改用「列脊」连接 + 顺时针扇区收窄

用户接着报「鱼骨子节点也有问题，矩阵等等都有这个问题」。查下来是**上一轮修的另一半**：
上一轮只保证「连线不穿框」（穿到别的节点就绕到外侧），但绕出来的是一个大钩子
（`M 386 156 L 386 146 L 367 146 L 367 90 L 413 90`）——不穿框了，却既不像时间轴也不像鱼骨。

- **新增连接类型 `spine`（列脊）**：父节点 → 先离开一小段 → 走到脊 → 沿脊走 → 横进子节点侧缘。
  一条脊 + 一排短横线 = 一份列表（Xmind 的时间轴 / 鱼骨就是这个形状）。
  - 脊的位置按**整列的所有子节点**算，不是只看当前这个：某个兄弟被手动拖偏之后，
    按单个子节点算出来的脊会落在它的范围里，连线又从它身上穿过去。
  - 「离开父节点的一小段」走在父节点与最近那个子节点之间的空档里，免得蹭着格子边缘。
  - 两个方向都支持：子节点在正上/正下方（刻目 / 骨刺 / 格位）脊是竖的；
    在另一列（树状表格：每层一列）脊竖在父列与子列之间。
- **接入**：矩阵（表头仍用母线，格子用脊）、鱼骨（骨刺用脊，主脊与骨刺本身不变）、
  时间轴（刻目不变，刻目下的列表用脊）、树状表格（全部用脊——以前是一束斜线）。
- **顺时针（放射状）**：子节点扇区收窄到最多 90°。以前只有两个分支时每个分支独占 180°，
  子节点被摊到 180° 以上——既不像顺时针图，连线也拉成长斜线横穿画面。
- **自检形状补到 9 种**（新增「两分支各两子」＝用户截图那张）：
  9 × 14 = **126 个组合**全部无节点重叠、无连线穿框（自检 **2293 项**）。

### 修复 · 连线不再穿过节点（矩阵 / 时间轴 / 鱼骨 / 组织架构的「错位」是同一个缺陷）

用户报的四处「错位」（矩阵的竖线穿过第一个格子、时间轴的刻目、鱼骨的骨刺、
组织架构行的错位）**根因相同**：连线只按父子两个节点算路径，兄弟排成一列时，
连到"更远那一格"的线会从"更近那一格"身上直插过去。

- **折线连线加「竖井绕行」**（`layout/core.ts`）：某一段穿过别的节点时改走竖井——
  从父节点先离开一小段、横到竖井、沿竖井上下、再横回子节点；竖井取「挡路节点的外侧 ±8px」，
  左右（上下）各试一条取更短的，两条都不行才退回原路径。一次覆盖所有"一列兄弟"的结构：
  矩阵、时间轴、鱼骨、树状表格、组织架构。
- **矩阵列**补「与父节点的最小间隙」：矩阵是唯一读手动 y 偏移的列布局
  （其它列的摆放在 `placeVerticalColumn` 里根本不读偏移），把格子往上拖到贴住父节点时，
  任何绕行都只能压着它过。
- **组织架构行**：行内**不认纵向偏移**（与矩阵「横向钳在列内」同一套语义的转置）。
  一行里的兄弟必须同基线；认了它，这一格就从那一行里挪出去（用户截图里的"错位"），
  拖得狠一点还会直接压到父节点身上。
- **放射状（顺时针）**：只有一个分支时不再给它整圈 2π 扇区——否则子节点被摊到 180° 以上、
  跑到中心主题**对面**，连线横穿中心。没有别的分支可避让时给一个直角扇区。
- **新增常驻审计**：自检按「8 种文档形状 × 14 个结构」全铺一遍
  （宽 / 深 / 参差 / 长标题换行 / 空标题 / 手动拖过），任何组合都不许重叠、
  不许连线穿框（**+112 项断言**，另加组织架构行基线 3 项）。

### 变更 · 结构改成「整张画布」的属性（去掉分支级结构）

- **为什么**：分支上能各自带结构时，一棵树里会混着几套排布——用户看到的画面是
  「主干是对的、下面那截乱」（截图：鱼骨图主脊正常，某个分支的子节点却按另一套结构堆着）。
  结构本来就是一整张图的观感，分散到分支上既难理解、也难做对。
- **三处口径统一**：
  - **布局层**：`subtreeExtent` / `connectTree` 一律按中心主题的结构算；
    子主题自己声明的 `structureClass` **不再参与布局**（字段保留：导入的文件里可能带着它，
    另存时原样写回，数据不丢）。
  - **界面**：「结构▾」只写中心主题——不管当前选中谁，改的都是整张画布（提示里写明了）。
  - **AI**：`setStructure` 的 `address` 只接受留空或中心主题，指定某一支**会被拒绝并说明原因**。
- **删掉的代码**：分支级结构的分派链（`layout/subtree.ts`、`layout/fishbone-subtree.ts`
  与 `stack.ts` / `orgchart.ts` 里的分支分派）——它们只为这个功能存在，留着就是死代码。
- **断言反转**：原来两组「分支级结构」断言改成新不变量——
  「给子节点写 `structureClass` 后所有坐标一字不差」（另加：字段仍在、`setStructure` 不碰子节点）。

### 修复 · 画布拖拽（掉帧 + 落点判定）与「恢复自动布局」入口收敛

**拖拽**（三个确证缺陷 + 一处判定错误）：

- **掉帧**：节点拖拽是唯一没有合帧的高频路径（滚轮 / 平移 / AI 写入都合帧了），
  高刷屏上 `pointermove` 能到 120~160 次/秒，每次都走一整遍落点裁决并重渲染整个画布。
  现在改成「只记最新位置、每帧最多算一次」；松手前先把最后一格结算掉，落点不会停在上一帧。
- **每帧重复的几何计算**：吸附候选与各自的可吸附区域改在拖拽开始时算一次
  （区域只依赖布局，而布局在拖拽期间是冻结的）；拖拽提示的标题不再每帧全树遍历。
- **过期的"非法落点原因"**：该变量只在声明处初始化过、没有每帧重置——
  先撞上非法落点、再移到合法位置时，底部会一直挂着那条过期提示。
- **有些节点怎么拖都只提示"成为子主题"**：同级方向被否决时（平衡思维导图的一级主题左右分列）
  分区函数收到 `null` 会恒返回 `child`。现在与「可吸附区域」共用同一套轴：拿不到同级方向就
  退回"这一层往哪边排"，两处不会再给出互相矛盾的答案。
- 顺带更正 `DROP_HYSTERESIS`（落点迟滞）的单位漂移：代码按**屏幕像素**比较，
  而注释与 `drag-snap-research.md` 写的是"世界单位、随缩放折算"——手感取决于手走了多远，与缩放无关。

**入口收敛**：「恢复自动布局（选中主题）」「全部恢复自动布局」两个按钮 + 菜单里同名两项
（三个入口、两种实现，其中两个完全相同）合并成**一个**「恢复自动布局」：
选中了自由摆放的主题就只恢复它们，否则恢复整张画布，结果用提示条说清（含个数，可撤销）。

### 新增 · 破坏性操作可以选「以后不再询问」

- 确认框里多一个勾选，按操作种类分别记忆：**删主题（含整个分支）/ 删关系线·边界·概要 / 合并同名主题**。
- 记忆落在 `settings.json`，并能在「AI 设置 → 破坏性操作的确认」里**全部恢复询问**——
  不允许出现「问了也白问、还改不回来」的状态。
- 默认**不记住**：确认框的意义就是第一次要问。设置文件被手改坏时，不认识的种类会被丢掉
  （宁可多问一次，也不让一条脏数据把确认框永久关掉）。
- 顺手把三种破坏性操作收进**唯一来源** `DESTRUCTIVE_WRITE_KINDS`：规划层的 `destructive` 标记、
  确认框、「不再询问」的范围都从它取——三处各写一份的话，将来加一种破坏性操作总会漏一处。
  补 7 项断言：清单成员、中文名非空、脏数据清理去重、默认不跳过任何确认。

### 清理 · 删掉死代码里的旧提示词（回归「单一事实来源」）

- `buildGenerateMessages`、`buildExpandMessages`、`buildPolishMessages`、`parseFlatList`、
  `cleanPolishedTitle`、`GenerateDetail` 六个导出**全仓零调用**（「AI 生成导图」对话框删除后失去入口），
  里面却还留着「每个节点不超过 12 字」这类**与现行规格冲突**的旧要求——留着迟早互相打脸。整体删除。
- 出图提示词现在只剩一条在用的路径（文档 → 导图），并补上断言：
  覆盖全部章节、写具体内容（保留数字/结论）、解释成子节点（不用备注行）、禁止「XX 的概述」空节点、
  分段读**只依据这一段**（防跨段瞎猜）、合并阶段去重归位。
- 自检 **2133 项**全绿（删掉 27 条死代码断言、补 6 条文档路径断言）。
- 顺带把**内容质量判据抽成唯一来源**（`QUALITY_CHECKS`）：聊天层与文档出图规格共用同一份。
  以前两处是并行措辞（意思一致、字面不同），改了一处另一处就悄悄过期——模型会同时看到两套说法。

### 重构 · 聊天系统提示词重写（分层 + 主流提示词工程技巧）

以前是 15 条平权规则一个扁平清单，问题：无优先级、内容重复（3/4/15 都在讲"不确定别猜"）、
靠形容词与数字而非可判定判据、动态数据位置破坏 prompt 缓存。这次按主流做法重构：

- **分层**：身份 → 【冲突裁决顺序】→【不可协商底线】→【工具与输出协议】→【内容质量判据】→
  【工作方式】→ **场景模块（按任务注入）** → 【动态数据】→【安全声明】。
- **新增冲突裁决顺序**：正确与诚实 > 用户明确指令 > 先问清再动手 > 覆盖与具体 > 措辞风格——
  以前模型没有裁决依据（「直接简洁」与「至少 100 节点」互相打架）。
- **形容词换成可判定判据 + 正反例**：「具体 / 行家 / 外行味」→ 三条能自检的检查
  （有信息量 / 是事实不是评价 / 可操作），并给**不合格示例与合格示例各一条**。
- **证据化自检**：收尾必须把 `getDocStats` 的数字写进总结、`findIncompleteNodes` 的清单逐条处理——
  不再接受"检查过了"这种自证。
- **按任务类型注入模块**（`classifyTaskIntent`）：生成类 → 生成规格（含档位）；编辑类 → 编辑模块；
  判不出来 → 「未判定」兜底。**小任务不再背 100+ 节点规格**（省 token、不跑偏）。
- **修 prompt 缓存自伤**：动态数据（骨架 / 选中 / 上轮改动）以前排在静态规则**前面**，
  每轮把后面内容挤出前缀缓存；现在挪到最后。【安全声明】紧邻数据之后。
- **修掉一处自相矛盾**：长文档分段读要求「用 `> ` 摘原文」，而文档出图规格禁止 `> `——
  现在统一为「单独写成子节点」。
- 生成规格删掉与静态层重复的"工作方式"（重复会稀释规则权重），只留档位规格 + 本档自检强度。

### 优化 · 聊天面板：输入区放大 + 思考强度就地可调 + 上滑不再被拉回底部

- **输入框放大**：2 行 → 4 行（最小高度 84px，最多长到 180px 再滚动），宽度占满整行；
  功能按钮（挂文档 / 粘贴剪贴板 / 发送）**移出输入行、单独一行**，文字不再和按钮挤在一起。
- **思考强度就地可调**：生成质量档位（省 token / 标准 / 更细）搬到输入框旁的下拉里，
  改完**下一轮请求立即生效**并写回 AI 配置（与「AI 设置」里的那一项是同一个值，不会各说各话）。
- **修复：模型输出时无法上滑看历史**。以前是**无条件**滚到底——每一片增量都把正在翻历史的用户
  拽回底部。现在只在用户本来就贴着底（距底 < 40px）时才自动跟随；上滑期间不再打扰，
  输入框上方出现「**回到最新**」按钮，点一下回到底部并恢复跟随；自己发消息时照样自动到底。

### 新增 · 生成质量档位（min / high / max）

「AI 设置 → 生成质量档位」三档可选。**档位只改「要求的规模与深度」，不改 token 上限**——
「单次输出上限」是旁边单独一项，两件事分开，避免悄悄替用户加预算。

| 档位            | 标准          | 规格                                                                                                                       | 自检                                       |
| --------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| **min**         | 及格（60 分） | 骨架完整（行家板块一个不漏）、每点 8~~20 字、每板块最多两层、40~~80 节点、尽量一次写完                                     | 轻量：只看有没有空壳板块                   |
| **mid**（默认） | 80 分         | 完整覆盖 + 具体内容 + 每板块配例子、100+ 节点、80 分三件事                                                                 | 覆盖率 + 叶子 + 三件事                     |
| **max**         | 90 分         | 在 80 分之上加三层深度：每板块 ≥3 层、叶子补「易错 / 边界 / 实操 / 对比」两类、每板块 ≥2 例子、易混概念成对对比、200+ 节点 | 最严：含**通读找「外行味」节点**并当场重写 |

- 三档共用底线：行家骨架、解释直接成子节点（不写备注行）、例题诚实标注（自编不得标「真题」）、
  先计划 → 逐步反馈 → 收尾自检。
- 档位存在 AI 配置里（`tier`）；坏值自动回退到 `mid`，改名前的旧写法 `high` 也按 `mid` 认。
- **就地可调**：聊天输入框下方的按钮行里就有「思考强度」下拉，改完写回同一份配置，
  下一轮请求立即生效（不必再进设置）。

### 约束 · 信息不足以判断就问用户（第 15 条）+ 真题诚实条款 + 数量达标核对

- **先问再动手（通用原则）**：动手前先自问「用户想要什么，现有信息能唯一确定吗？」——
  目标不明、范围不清、多种合理解读、关键参数缺失（考试科目 / 级别 / 版本 / 领域 / 详略）都算不足，
  用 askUser 问清（给候选）再动手。原来只有「节点指向不明」会问，**任务本身的大方向没人管**——
  真事：用户说「生成计算机四级复习大纲」，模型擅自挑了网络工程师方向写 100+ 节点。
  同时防止另一个极端：能唯一定位、能合理推出的直接做，不拿提问当拖延。
- **真题诚实条款**：只有确实记得的历年考题才能标「真题」（注明年份）；记不准的一律标「模拟题」——
  **把自编的题标成真题是造假**（真事：上一版图里的「真题示例」全是编的）。
  对话框详细模式的旧示例本身在教模型这么标，一并改掉。
- **领域通用「80 分标准」**：生成提示词全面修缮——第一层必须按**行家共识的划分**铺全部顶层板块
  （考试按考纲、金融按市场/产品/风险/监管、软件按模块与层次、文书按章节……），一个不漏、不挑熟悉方向；
  数量只是参考，**合格标准是覆盖率**：没有空壳板块、叶子都是具体知识点。收尾自检照此核对，
  发现空壳板块就回去补透再总结。
  **80 分 = 及格线之上再过三件事**：① 层次有逻辑（并列 / 流程 / 对比各就各位，对比写成对节点）；
  ② 重点有区分（核心 / 高频 / 易错单独成节点并说明为什么重要）；③ 叶子粒度可操作（读到就能用）；
  例子每个板块都要有；术语必须准确，不许编行话。两条路径（聊天 agent 与对话框详细模式）都已同步。

### 修复 · 模型退化循环熔断（同一行连吐几百遍）

- **现场**：flash 档模型在超长上下文（实测一轮输入 109k token）里退化成逐字复读——
  「（我来执行）。」连吐几百行，既烧钱又出不了活，最后靠自我纠正才爬出来。
- **修**：流式正文里**同一行逐字复读达 10 次**即熔断止损——停止读取（多读一行就是多烧一笔钱）、
  **裁掉复读尾巴**（不进正文、更不进下一轮上下文，否则会把下一轮也拖进退化），界面如实提示
  「重试通常可恢复；反复出现请换更强的模型」。
- 判据刻意保守：完整成行、去空白后逐字相同、连续达门槛——正常内容里连续多行完全相同几乎不存在。
- 顺带：每轮记录上下文体积（`[chat] 本轮上下文：N 条消息 / M 字符`）——
  「越聊越贵」与退化的共同根源是**上下文膨胀**，先有数据再谈压缩。

### 新增 · 思考过程直播（不再对着转圈干瞪眼）

- 推理模型的思维链（`reasoning_content` / 混在正文里的 `<think>`）以前被**直接丢弃**，
  界面只剩一个转圈加「正在思考…」。
- 现在边想边显示：思考中**展开直播**，正文一开始自动收起成一行「已深度思考（点击展开）」，随时可点开回看。
- 模型不吐思维链时（如 deepseek-chat）就没有这一块，行为与从前一致；想看直播换 deepseek-reasoner 等推理模型。

### 修复 · 「AI 写的备注怎么打开」——备注图标可点了

- 画布上节点的备注指示图标以前**点了没反应**；看备注要去「节点属性」面板，藏得太深，用户从没发现过。
- 现在点 📄 图标直接打开面板**并聚焦备注框**；超链接 / 附件图标同样可点。

### 约束 · AI 不再写备注，内容直接写成节点

- 用户要的是**看得见的内容**：大纲系统提示 / 详细模式 / 文档出图规格 / agent 第 13 条 /
  `insertSubtree` 工具描述全部改为「解释与细节**直接写成子节点**（要点 / 公式 / 易错 / 例），不要用 `> ` 备注行」。
- 解析器仍兼容 `> `（与 Markdown 导入同一套语法）：**存量文档的备注都还在**，也能点图标查看。

### 约束 · 反「概念堆砌」

- 三处提示词加入硬约束：**禁止**「XX 的基础知识 / 概述 / 简介」「了解 XX」这类不含信息量的节点；
  定义带关键特征、公式带表达式与适用条件、对比写出差异点；考题类主题写真题（题型 / 考法 / 答案要点）。
- agent 的自检提示从「查缺备注」改为「查**叶子节点**」——细到不能再细才算讲透。

### 优化 · AI 入口收敛：聊天是唯一入口

- 工具栏 AI 菜单只留「打开 AI 聊天…」与「AI 设置…」。原来并列的「一键生成导图 /
  按文档生成导图 / 扩写子主题 / 润色标题」全部移除——这些聊天里都能做
  （31 个工具覆盖生成、扩写、改写、挂文档问答），两套入口只会让人犹豫该走哪个。
- 聊天面板输入框上方那排「会改画布」的任务按钮（润色 / 扩写 / 生成）一并移除：
  与聊天写工具三重重复，而且聊天对写操作**自有确认条**（破坏性操作先问再做），预览能力没有丢。
- 「按文档生成导图」的保留入口：把 docx / md / txt 直接**拖进窗口**（原来就有）；
  拖进聊天面板则是挂上问答。
- 原「AI 生成导图」对话框整体移除（已无任何入口，不留死代码）。
- 顺手修输入区：占位文字缩短（原来带「Shift+Enter 换行」在窄面板里折成两行还出滚动条），
  快捷键说明挪到悬停提示。
- 注意：聊天里让 AI「生成新导图」现在会写入**当前文档**（「新窗口」目标随对话框移除）；
  想要独立一份，生成后「另存为」即可。

### 修复 · 自动续写不再「静默丢节点」

- **症状**：一句命令生成详细图时，偶尔会少一个考点——不报错、不留痕，
  表现和「AI 又漏了一个」一模一样，用户无从发现。
- **两处根因**：
  1. **续写指令自相矛盾**：既说「从中断处继续写完剩余部分」，又说「不要重复已经写过的内容，
     直接从下一行接着往下写」。对「断在一行中间」这种最常见的情况，后一句等于**放弃那半行**——
     模型乖乖另起一行，那个节点的内容再也补不回来。
  2. **拼接时无条件丢掉前一段的最后一行**：截断点正好落在行尾时，被丢掉的是一整行**完整**节点。
- **修法**：
  - 指令改成「最后一行没写完就**从行首完整重写一遍**；已经写完整就不要重复它」——
    让残行的完整内容一定出现在续写的第一行里。
  - 拼接改成**只在能确认模型重写了那一行时才丢掉它**（加长版 / 完整行被重发），其余一律保留；
    重叠不足 6 字不判为同一行（避免把「缓存 / 缓存策略」这类兄弟节点合掉）。
  - 每次续写留下现场日志（`[outline] 续写拼接：<关系>`，含被处理的尾行与续写首行），便于继续观测。
- **取舍**：宁可多留一个**看得见**的残句，也不静默吃掉用户要的节点。

### 新增 · 四个工具补齐「让 agent 掌控整个 SMind」

- **`sortSiblings`（写）**：同级子主题排序（按标题 / 标题长度，正序 / 倒序），
  可选**自动编号**（1. 2. 3. …，先去掉旧编号，不会叠成「1. 1. x」）。整理类任务的收尾常用。
  没被点名的子主题**一个不丢**，保持原有相对顺序排在后面。
- **`findDuplicates`（只读）+ `mergeDuplicates`（写 · 破坏性）**：按「同名」查重
  （忽略空白、标点、全角半角、尾部编号与「（补充）/ 副本」后缀），并额外列出
  「疑似近义」（一个标题是另一个的一部分）。合并时**保留内容最全的那一个**
  （子树大小 + 有无备注/代码/公式），把其余的**子主题搬过来、缺的备注/代码/公式/标签/标记补上**，
  再删掉多余节点——整批算**一步撤销**；组内存在父子包含关系时**跳过并如实说明**
  （否则会把子节点搬进自己的祖先里）。
- **`readDocument`（只读）+ 面板挂文档**：把文档**拖进聊天面板**（或点输入框旁的 📎）就挂上了，
  之后可以直接问「这份文档讲了什么 / 关于 X 它是怎么说的」。
  工具支持按**关键词**取片段（带行号，最适合问答）或按区间读一段（默认 6000 字，长文档会提示"还有更多"）。
  文档只读成文本（解析仍在主进程），**只在本次会话有效**，显示在输入框上方、可单独移除。
- **`exportOutline`（只读）**：让 AI 直接导出大纲（txt / md / opml），弹系统保存对话框由用户选位置；
  用户取消会**如实回喂**给模型（不会假装导出成功）。
- 工具总数 26 → 31（读 7 → 10、写 19 → 21）；自检 +48 项（排序的编号/撤销/不丢节点、
  合并的内容并入与祖先保护、查重的规范化、读文档的关键词与分段）。

### 新增 · 三个常用工具 + 「计划 → 工具 → 反馈 → 自检」工作方式（界面按时间线呈现）

- **`updatePlan`（计划工具，只读）**：模型动手前先写下 2~6 步计划，之后每完成一步更新进度。
  计划**显示在聊天里**（✓ 已完成 / ▶ 当前 / · 未开始），长任务（生成上百节点、批量补内容、
  重构结构）一眼就能看出"走到第几步、有没有跑偏"。它不碰画布，也不消耗试用回合。
- **`findIncompleteNodes`（覆盖度自检，只读）**：查出哪些节点**缺备注（解释）**、是叶子、缺代码块，
  返回**带句柄的清单 + 按分支汇总**（先补漏得最多的那一支）。
  「用户要详细、模型说"都写好了"」之间的差距，从此可以核对，而不是凭印象。
- **`setStructure`（写工具）**：AI 现在能切换结构（思维导图 / 逻辑图 / 鱼骨图 / 时间轴 / 括号图 / 矩阵图…），
  整张图或某一支都行——用户说"改成鱼骨图"不必再手动搬节点去模拟。
- **工作方式（系统提示词第 14 条）**：大任务 → 第一步 `updatePlan` 写计划 → 每步**简短反馈 + 更新进度** →
  全部做完**必须自检**（`findIncompleteNodes`，必要时 `getDocStats` 核对数量）→ 再给总结
  （完成多少处、自检结论、还剩什么及原因）。小改动（改标题、搬节点、换结构）不写计划。
- **界面：纵向时间线**（移植自备份分支的做法）。每一轮模型的解说在工具调用到达时**移进步骤区**
  （`💬` 开头、单独配色），正文只保留最后一轮的（= 自检与总结）。
  阅读顺序因此是：**计划 → 工具步骤 → 每步反馈 → … → 最终自检**，
  而不是"一堆工具痕迹在上、一段不知道属于哪一步的正文在下"。
- 顺手修掉试用计数的一个小漏洞：`askUser`（只提问、什么都不改）原来会被算成一次写回合，
  现在计数只认**真正改画布**的工具（`AGENT_CANVAS_TOOL_NAMES`）。
- 工具总数 23 → 26（读 5 → 7、写 18 → 19）；自检 +37 项。

### 新增 · 拖入文档，AI 读完按内容生成导图

- **拖一份文档进窗口**（或工具栏 AI →「按文档生成导图…」）即可：读取 → AI 细读分析 → 预览 → 落地。
  - 支持 **.docx / .xlsx / .pptx**（zip + XML 自研抽取，**不引入新依赖**）与
    **md / txt / csv / tsv / json / yaml / xml / html / 日志 / 各种代码**；
  - **PDF 明确不支持**并给出替代方案（另存 / 导出为 Word 或 TXT）——可靠的中文 PDF 抽取要额外的
    解析库，做不好就是"看着成功、满屏乱码"，所以宁可当面说清楚；
  - **编码自适应**：UTF-8 读出来有替换符时自动试 GBK / GB18030
    （Windows 上的中文 txt / csv 大量是 GBK，硬读会得到一片乱码）。
- **长文档分段细读 + 合并**：超过一段的量按**行边界**切成多段，逐段提取结构与要点，
  再把各段结果**合并去重、按原文结构归位**。一份几万字的报告根本塞不进一次请求，
  硬塞的结果就是模型只读了开头——那正是"分析得很粗"的来源。
  超出段数上限时**如实报出有多少字未纳入**。
- 输出与「按主题生成」同一套：详细大纲 + `> ` 解释行进备注、预览显示解释与「N 个带解释」计数、
  可落到**新窗口**或选中主题下（整棵树一步撤销）；被输出上限截断时同样**自动续写**。
- **文件读取全在主进程**（渲染层不碰文件系统）：字节经 IPC 送来，上限 32MB 文件 / 48MB 解包文本 /
  30 万字正文，超出如实告知。拖放分流：`.xmind/.emmx` 仍走「打开文档」，图片仍贴到选中主题，
  其余文档才进这条流程；不支持的格式**拦下默认导航**（否则界面会被那个文件替换，像"软件坏了"）。
- 自检 +52 项（格式识别、docx / xlsx / pptx 抽取、清洗统计、分段边界与丢弃计数、提示词）。
- **实跑验证**：真实 Word（7171 字，中英混排含音标）、真实 PPT（3084 字，按页）、
  真实 md 三份——抽取均正确。xlsx 只用夹具验证过（这台机器上没找到真实表格）。

### 优化 · 生成导图：全领域「详细规格」+ 一次命令上百个节点

- **详细规格变成默认、且全领域通用**（原来只按考题写：其它领域照样只给骨架）：
  聊天系统提示词第 13 条改为「**只要用户让你生成 / 写一份导图，默认就按详细规格来**」——
  覆盖完整结构、目标**至少 100 个节点**、每点 15~~40 字具体内容、用 `> ` 行写 40~~100 字解释、
  关键节点补例子（考题类写真题，其它领域写实例 / 案例 / 代码 / 数据）。
  对话框的「详细模式」同步改成全领域口径（不再只提"高频考点"）。
- **预算放大到能撑住一次生成上百节点**：
  `AGENT_MAX_ROUNDS` 16 → **40**、`AGENT_MAX_TOOL_CALLS` 60 → **200**。
  一次详细生成 = 读骨架 + 按分支分批 `insertSubtree` + 补解释 + 收尾，十几个轮次起步；
  16 轮会让多分支的生成写到一半被截断——那正是「只写了粗分」。
- **默认输出上限 8192 → 16384，并改成逐级降档重试**：
  服务商不接受时按 16384 → 8192 → 4096 → 2048 → 不发送 逐档后退，
  且**只在错误体点名输出上限相关字段时才降档**（其它错误原样翻译，不会被藏起来）。
- **对话框自动续写**：被输出上限截断时自动接着写（最多 3 段），按行拼接
  （丢掉截断处的半行、去掉模型重复的最后一行）——不再把半张图丢给用户、让他重写一遍提示词。
- **顺手修掉一个会放大消耗的缺陷**：试用计数原来**按「轮」**消耗
  （`consumeTrialTurn()` 在每个模型请求后都调一次）。一条命令跑 20 轮就会一口气吃掉
  20 个试用回合——「送 30 个写回合」实际只够一两次操作。
  现在渲染层为每个用户命令生成 `turnId`，主进程按它去重：**一条命令无论多少轮只算一个写回合**。
- 自检 +12 项（详细规格提示词、回合 / 调用上限、试用去重与集合上限）。

### 修复 · 「让 AI 写完整详细的导图，它只写完粗分」

**根因不是模型能力**，是三层我们自己的限制（每一条都有代码位置可查）：

1. **请求里从来没给输出上限**：`max_tokens` 一个字段都没发过，用的是服务商默认值
   （多数只有 1.5k~~2k），而「完整详细的大纲」（多考点 + 解释 + 真题）动辄 4k~~8k——
   写到一半被服务端**掐断**；更糟的是 `finish_reason` 虽然解析出来了，
   **主进程与渲染层都没有用它**，于是「被截断」和「正常说完」在应用里长得一模一样。
2. **对话框提示词写死了"简短"**：层级默认 3 层（滑块只能 2~5）、每个节点「不超过 12 字」——
   12 字装不下任何知识点，模型只能给标题词，出来就是一副骨架。
3. **聊天路径没有任何"内容密度"要求**：12 条行为规则里，规则 1 的「直接、简洁」管的是
   **回答语气**，模型却容易把它推广到节点内容上。

**修法**：

- `AiConfig.maxTokens`（默认 **8192**，可在「AI 设置 → 单次输出上限」调；填 0 = 不发送该字段）。
  服务商不认这个字段时**自动去掉重发一次**（只在错误体点名 max_tokens 时触发），不会把请求搞死。
- `finish_reason === 'length'` → 流式事件带 `truncated` → 聊天面板在气泡上**如实说明**
  「本回合输出被输出上限截断」，并告诉用户回复「继续」可接着写；生成对话框同样提示（含可操作建议）。
- **新增「详细内容」的载体**：缩进大纲里紧跟主题行的 `> 解释` 会成为该主题的**备注**
  （与 Markdown 导入器同一套语法）。于是解释、考点说明、真题解析**不占画布宽度**，
  但能搜索、能导出、能看见——生成预览里直接显示，并统计「其中 N 个带解释」。
- 对话框新增「详细程度」：**详细**（默认）= 覆盖全部高频考点 + 每点 15~~40 字 +
  `> ` 40~~100 字解释 + 关键考点补 1~2 道真题；**骨架** = 原来的短标题模式。层级默认 3 → 4。
- 聊天系统提示词新增第 13 条「内容密度」：要"完整/详细"时**一次 `insertSubtree` 交出多层完整大纲**、
  用 `> ` 写解释、关键节点补真题；分支多就分几次写，并明确「不要一次写完导致被输出上限截断」。
- 解析兜底**不再静默丢行**：长行阈值 24 → 40 字（"多逗号"那条 20 → 30），
  确实丢行时给出计数与原因——详细内容以前正是被这条规则悄悄吃掉的。
- 自检 +25 项（输出上限夹取、`> ` 解释行进备注、长行不再丢、详细模式提示词）。

### 优化 · 视口与渲染（四项视口修复 + AI 回合内的布局节流合帧）

- **滚轮 / 触控板平移不再逐事件重渲染**：每个物理事件都写一次 pan（实测 120–160 次/秒），
  每次都把整棵画布重渲染一遍——滚动时主线程被打满，同期运行的 AI 批量写入会被饿死。
  现在把位移累进局部变量、`requestAnimationFrame` 每帧最多落账一次（右键/中键拖拽同理），
  落点与逐事件处理完全一致。
- **复活 `TopicNode` 的 `memo`**：以前传给节点的 8 个回调每次渲染都是新函数，浅比较必然失败，
  memo 整个被架空——每次 pan 更新都放大成「全部可见节点重渲染」。回调提为稳定引用
  （实时值改走 ref）。
- **视角锁定不再和用户的手抢镜头**（用户报告的「画面一长就上下抽动一阵再恢复」）：
  跟随循环每帧把镜头往焦点主题拽 22%，而**滚轮滚动没有任何让位规则**（拖拽有），
  两股力对拉 → 抽动，直到 240 帧止损才停。现在滚轮 / 拖拽 / 缩放 / 居中 / 适应画布都算
  「用户接管视角」，跟随循环当帧让位；下一次选择或几何变化再重新咬住（与拖拽后的行为一致）。
- **折叠之后镜头不再失去中心**：被折叠收起来的选择已不在布局里（`nodeMap` 查不到），
  跟随循环会一直等一个永远不来的目标、90 帧后静默放弃——现在回退到中心主题。
- **AI 回合内的布局节流合帧**（`usePacedWorkbook`）：AI 批量写入时把重排合并到每 ~100ms 一次；
  节点文字/选中/悬停仍实时读最新数据，只是位置晚一拍。用户自己的操作不在回合里，零延迟。
- 视角锁定的三类异常告警改为**同类只报一次**：它们处在每帧路径上，而渲染层 `console.warn`
  会被主进程转发进应用日志（同步落盘）——持续异常时会变成每秒几十次写盘的**新卡顿来源**。

### 修复 · 切换文档时「对话消失、但回合还在跑」（跨文档误写）

- 回合状态（轮数 / 消息线 / 待执行队列）都在聊天面板的 ref 里，**不随文档走**；
  而文档路径一变（首次保存 / 恢复 / 另存为）面板就会清空或加载新路径的历史——
  于是界面上「对话消失了、但还在第 14 轮」；更糟的是写工具作用于**当前激活文档**，
  继续跑等于可能把改动落到另一份文档上。
- 修：文档一变就**中止进行中的回合**（取消流、清队列与待确认、结算撤销事务、写日志），
  然后按新路径加载它自己的历史。消息不再无声消失，跨文档误写也被堵死。

### 安全 · API Key 不再以明文留在配置文件里

- 写入改用**系统安全存储**（Windows 走 DPAPI，密钥绑定当前用户账户）：`ai-config.json` 里
  只有密文 `apiKeyEnc`，**不再出现明文 `apiKey`**。
- **读取兼容老文件**，并在启动时自动迁移一次——升级不会让用户的 Key 失效（迁移幂等，
  只对存在明文的文件动手）。
- 换了机器/账户导致解不开时，当作"没有 Key"让用户重填，而不是抛错把面板打挂；
  Linux 没有 keyring 时退回明文（能用比"安全但不能用"重要）。

### 新增 · 三期的上下文压缩（长对话不再「失忆」）

- 以前是**截断最近 16 条**：模型会忘掉之前干过什么，用户说「继续」它就从零重读一遍导图
  （大导图上就是把同一种折腾重复一遍）。
- 现在：**最近 6 条保留原文**，更早的折叠成一段「此前做过什么」注入消息线。
- 摘要里的「我做了…」**取的是工具条目（执行记录）**，不是模型自述——自述会说错，记录不会。
- **零模型调用**：纯函数 `compressHistory`，确定性、可自检（新增 14 项断言）。
- 摘要超长时从**最早**丢（越近越重要）；注入时带一句「别凭这份摘要去改节点，细节请重新探查」
  ——因为本项目的判断是「**文档本身才是 agent 的持久记忆**」，对话可以激进压缩。

### 修复 · 致命：代码块里出现 `@` 或 `\` 会让界面**永久冻死**（渲染进程 100% CPU）

- **现象**：AI 批量补全 Python 代码块后，往画布**下方滚动、滑到一半整机卡死**；
  日志里只有 `unresponsive`，只能强杀进程。小文档（23 个节点）同样稳定复现，
  而且**不调用 AI、只恢复文档再滚动**也会卡死。
- **真凶**：分词器标识符分支里 `IDENT_START` 认 `@` 与 `\`，而 `IDENT_PART` 不认——
  只要一行里出现 `@`（Python 装饰器 `@functools.wraps`、Java 注解、JSDoc 的 `@param`、
  邮箱地址…）或 `\`（Windows 路径），内层 `while` 的游标就**停在原地**，
  外层 `while` 于是无限自旋。它只在「那块代码**第一次进入视口、首次分词**」时才触发，
  所以表现为「滚动到一半卡死」，且与文档大小无关（纯内容依赖）。
- **修法**：
  1. 标识符分支在游标没推进时**退化成「吃掉这一个字符」**——保证推进，也保证拼回原文；
  2. 两个扫描器（`code` / `data`）各加一道**推进保证保险丝**：任何分支让 `i` 原地打转就强制前进一格。
     这类「匹配了却不推进」的写法以后不可能再冻死界面。
- **验证**：**14 种语言 × 17 种恶劣输入 = 238 个用例零卡死、全部逐字节还原**；
  同一份文档（此前二分出 2 个毒块）现在 20/20 全通过；自检 +14 条回归断言（**1884 项全绿**）。

### 修复 · 探针在「永久死循环」这种最坏情况下也要能留证据

- **阶段一变就立刻落一行**（相邻重复去重）：同步死循环不让出主线程，心跳会停、看门狗也没机会说话，
  这行「进入致命阶段的时间戳」是唯一能拿到的现场（配合主进程 `unresponsive` 时刻即可算出跑了多久）。
- **回合结束后诊断再保持 120 秒**：实测冻结发生在「AI 生成完 → 用户开始滚动」那一段，
  以前回合结束就关掉诊断，白丢现场。
- **常驻「主线程停顿」看门狗**：一恢复就写 `主线程停顿 XXXXms（停顿前最后阶段：…）`，
  给出以前最缺的**时长**；打包版、诊断未开也有效。
- 修掉现场转储的**异步拒绝未捕获**（`diagDump` 失败会在控制台刷 `Uncaught (in promise)`）。
- **看门狗区分「后台节流」与「真停顿」**：窗口被遮挡 / 最小化时 Chromium 会把定时器压到 1Hz，
  那是节流不是卡顿——之前空闲窗口里整屏都是「主线程停顿 1000ms」，把真现场淹了。
  现在页面不可见不报、连续多拍都慢（基线本身就慢）不报，
  只有「单独一拍慢」或「远比当前节奏更慢（≥3 倍）」才算真的堵住主线程。

### 新增 · 卡死取证升级（为「AI 一次性补全 10–15 个代码块就卡死」这条复现埋点）

- **探针从「每秒心跳」升级为「阶段进出一对 + 耗时 + 总量」**（`dev/stage.ts`）：
  - `进入 X` 在每个阶段**进入时立刻落一行**——卡住的阶段永远不会自己报完成，
    所以**日志最后一条就是案发现场**；
  - 单次超过 120ms 立刻记 `慢 X …ms`（卡顿的构成单元一眼可见）；
  - 每秒心跳带计数与总量（如 `代码块渲染×288`、`代码块 span 共 45 万`）——
    这正是「写入次数 × 代码总量」二次放大的直接证据；
  - AI 回合结束打一行**耗时归属**：`画布布局×12 共 3400ms(最慢 1400) · 应用写意图×12 共 210ms …`。
- **关键判别点**：`进入 画布布局` 与 `画布提交完成` 两头都打点——
  最后一条落在前者＝卡在**计算**，落在后者之后＝卡在 **DOM 提交**（token span 爆炸）。
- **打包版也能取证**：`[stage]` 通道本来就由主进程落进应用日志；这次顺带修掉一个隐患——
  主进程的 `console-message` 处理器只认旧签名，Electron 32+ 换成事件对象签名后可能**静默失效**，
  现在两种签名都兼容（这条通道是渲染进程被堵死时**唯一可靠的出口**）。
- **开关纪律**：日志只在开发版或 AI 回合进行中才写；日常编辑零开销。转储自带
  「序列化超过 25ms 就退回节流」的自适应保护。
- 新增 [`docs/diag-freeze.md`](docs/diag-freeze.md)：现场文件位置、探针样例、
  **判读表**（最后一条日志 → 结论 → 下一步）、离线二分复现流程。

### 布局收尾 · 偏移避让补齐 + 边界/概要预留空间 + 两处布局热点

- **手动偏移避让补齐到全部家族**（文档里「其余家族留待后续」那条）：
  组织架构行做**横向右推**、矩阵做**同列下推**；括号图与树状表格本来就复用共享堆叠路径，
  自动继承。顺带修掉一个真缺陷：**顶层矩阵以前完全不认手动偏移**——它用的是自己那套 walk，
  既不吃 `position`，也不会有避让（拖动等于没反应）。矩阵现在纵向偏移生效、
  横向钳在列内（表格语义下不该挪到隔壁列上）。
- **概要/边界参与布局空间预留**（Xmind 同款行为）：把「边界/概要向外占用的量」
  交给布局，与绘制共用同一批常量，所以「留出来的白」与「画出来的黑」不会各说各话。
  竖直堆叠家族（思维导图各向 / 逻辑图 / 树形图 / 树状表格 / 括号图）按上/下留白，
  组织架构行按整行留白 + 给右邻居留出括号宽度。**未覆盖**：鱼骨图、时间轴、放射图、
  矩阵行距（覆盖层朝向各不相同，留待逐结构验证后接）。
- **布局热点两处**（审计发现）：
  - `addOverlays` 以前**每个**边界/概要都各自递归一遍自己区间的子树（B 个边界 = O(B×节点数)），
    现在一次后序遍历建好「子树包围盒」索引，取任一区间只花 O(区间长度)；
    边界还顺手去掉了「同一个 range 解两遍」的重复解析。
  - `maxDepth`（鱼骨/时间轴用来定主脊长度）以前**无缓存**，深嵌套且逐层声明同类结构时
    退化成 O(节点数 × 深度)；现在按 id 缓存。
- **不做的**：真正的「仅子树局部重算（增量布局）」——`known-issues.md` 已论证
  连线、坐标归一化与按层/行/列的全局聚合本质上都是全局的，需要一次独立重构，
  不在本批内。
- 自检新增 **10 条断言**（偏移避让 5 条 + 边界/概要预留 5 条），
  且都做过「关掉修复就变红」的灵敏度验证：**1870 项全绿**。

### 优化 · 工具层「每次写入都放大一次」的成本（顺「加了工具之后才冻结」这条线索排查）

- **高亮缓存有个病理口子**：单个代码块超过 20000 字符就**不进缓存**——AI 往一个节点里写
  整份文件后，此后每一次重排（每次 store 改动、每敲一个字）都会把这段大代码**全量重新分词**，
  正是「内容依赖型」的形状。改成按**总字符预算**限流（64 条 / 4M 字符）：
  大块也能命中，内存仍有上界。
- **批量移动 O(k×N) → O(N+k)**：`moveTopics` 以前逐条调 `moveNode`，而每条都要跑一次
  `settleAfterMove` → `pruneOverlays` 的**全树扫描**——一次最多 200 条就是 200 遍全树，
  是「AI 批量整理大导图」时的固定放大器。新增 store 批量入口 `moveNodes`：
  一次写入落完、覆盖层只在末尾清一次，终态与逐条调用完全一致（自检补了等价性断言）。
- **折叠徽标计数曾经每个节点都要算一遍整棵子树**（O(节点数 × 深度)）：`countDescendants`
  加身份缓存——immer 下改过的子树必然换引用，未改动的直接命中。
- **代码块是「一个 token 一个带内联样式的 `<span>`」**，而 AI 每写一次都会让布局对象换新、
  代码节点必然重渲染——不缓存就是百万级元素重建。现在把元素数组按（内容, 语言）缓存：
  内容没变时 React 直接跳过整棵子树。实测单块 2KB / 10KB / 50KB 分别是
  414 / 1918 / 9438 个 span；24 个代码块 × 60 次写入下，未缓存时的重建次数是
  59.6 万 / 276 万 / **1359 万**次——这就是「24 主题的小文档也能冻住」的原因：
  决定成本的是**内容量**，不是节点数。
- **取证转储的自我保护**：强制转储在大文档下每次写都要同步序列化整份 workbook，
  实测单次超过 25ms 就退回节流模式——取证工具不能自己变成新的卡顿来源。

### 新增 · 冻结取证工具就位（自取证转储 + 内容二分器）

- **自取证转储**：AI 回合期间把现场（消息线含全部工具参数 + workbook + 阶段）**节流 2 秒**
  写到 `%APPDATA%/smind/diag/last-state.json`，每次写意图落地后**强制**落一份。
  历次冻结都发生在「写意图落盘后的渲染」，而未命名文档没有自动存档、聊天历史也不落盘——
  强杀进程会把毒内容一起带走，下一轮只能从零猜；现在冻结后现场一定还在磁盘上。
- **内容二分器**（`npm run diag:freeze [xmind 路径]`）：对文档里的**每个代码块**跑渲染同一套
  流水线（分词 / 代码块指标 / 标题寻址匹配），每块一个独立进程 + 4 秒超时——
  哪个块不返回，哪个块就是毒块，并把内容写到 `.tmp-check/poison-N.txt`。
- 移植时修正两处实跑即崩的问题（工具此前从未被真正验证过）：
  `content.json` 的结构假设写成了 `.sheet.rootTopic`（实际是画布数组、元素直接带 `rootTopic`）；
  自动存档路径写死了用户名，改为按 `APPDATA` 推导。
- 已用构造样本端到端验证：2 个代码块逐块通过。
- **自测钩子（补上此前唯一没被真正执行过的分支）**：`DIAG_FAKE_SPIN=<索引>` 让指定块
  **永不返回**，用来把「超时判毒块」这条**判决路径**真的跑一遍——真触发过它的自旋 bug
  已经修掉，这条路径长期处于"逻辑上应该对、从没被跑过"的状态。
  实测：`#1 OK / #2 ★★★ 毒块`，内容正确落盘到 `.tmp-check/poison-2.txt`。

### 修复 · 随回退丢失、按提交逐个挑回

- **折叠徽章的数字被截成「2…」**（视觉 bug，AI 批量补全后必现）：徽章绝对定位在节点右缘
  （`left: 100% - 8px`），收缩宽度只剩 8px 可用——折叠数一到三位数就被截断。
  显式 `width: max-content` + `white-space: nowrap` 让它按内容撑开；悬停提示补上精确数字。
- **状态栏常驻「视角锁定」开关**：以前只在开启时才出现——关着就隐形，用户找不到入口。
  现在常驻显示，关闭态灰显但仍可点（标题里写明快捷键）。
- **视角锁定开关跨会话记忆**：`viewLock` 原来是纯会话状态，每次重启 / 新开文档都回到
  设置默认值——用户刚打开开关，窗口一重启就"消失"。现在用 localStorage 记住最近一次选择，
  启动 / 新建 / 打开文档都恢复它；「启动默认视角锁定」设置只在从未动过开关时作为初值。

### 优化 · AI 批量写入后的卡顿与模型往返（内容依赖型卡顿的两条主线）

- **代码块高亮加了渲染缓存**：以前每次全页重排（AI 批量写入、任何无关节点改动）都会把
  每个代码节点里的整段代码**重新逐字符分词一遍**——纯 CPU、零缓存，代码越多卡得越久，
  与「内容依赖、分钟级」的冻结症状吻合。现在分词结果按 `(语言, 内容)` 复用
  （固定容量 64、超大代码不入缓存），分词结果是只读数据，行为完全不变。
- **已执行工具调用的参数压缩成短摘要**：轮内每执行完一个工具调用就把**整条消息线**
  原样重发给模型——`setCode` 的整段代码、`insertSubtree` 的整段大纲全都原样带着，
  几十个调用就能把单轮提示词撑到 60k+ token，光让服务端读一遍就要一两分钟
  （日志实测单轮往返 125 秒：UI 没死，但像死了）。
  现在成功执行的调用只保留开头 200 字符加省略说明（执行结果本来就在对应的工具消息里）；
  失败/未执行的调用不压缩，保留模型自我纠错所需的完整参数。
- 顺带：代码块结构的类型统一到共享的 `TopicCode`，消除三份手写内联定义。

### 修复 · 弹窗与粘贴（AI 设置 / 发送报错）

- **鼠标划出弹窗就把设置框关掉**：遮罩原来只判 `click`——用户按在弹窗内部、把鼠标划到外面
  才松开时，浏览器仍会往共同祖先（遮罩）冒泡一个 `click`，于是被当成「点了空白处」。
  现在要求**按下与松开都落在遮罩上**才算点了遮罩（对所有弹窗生效）。
- **保存后自动关闭**：AI 设置保存成功即关掉，不再要求用户再点一次「关闭」
  （「测试连接」里那次隐式保存不关，避免测试完就被弹出去）。
- **粘贴长内容后发送失败只报「对话内容无效」**：额度按真实使用放宽
  （单条 6 万字 → **20 万字**，条数 120 → **400**），并且报错说清是哪种超限、该怎么办；
  同时把 Electron 的 `Error invoking remote method …` 包装剥掉，只留人话。
- **剪贴板里是图片时粘贴静悄悄没反应**：现在会明确提示「聊天目前只能发文字」，免得被当成功能坏了。

### 新增 · 二期第二批写工具：AI 能碰「导图的另一半」

- **8 个新写工具**：连关系线、加边界、加概要、改这三者上的文字、删掉它们、设置标记图标、加/去标签。
  在此之前 AI 只能改**主题树**——关系线、边界、概要这些"表达关系与强调"的部分它完全碰不了。
- **新增只读工具 `listAttachments`**：列出画布元素（含 id）与**可用标记 id 清单**。
  这些元素没有标题可寻址，模型必须先用它拿到 id；标记清单不查，模型就会自己编一个不存在的 id。
- **语义按「模型安全」设计**：现成的 `addRelationship` / `addBoundary` / `addSummary` 读的是
  **用户当前选中**（AI 一改选中就搅乱用户选区），而且是「再点一次就删」的开关语义
  （模型重试一次就把线删了）→ 新增 4 个**不依赖选中、幂等**的 store 动作：
  `connectTopics` / `addBoundaryFor` / `addSummaryFor` / `setMarkers`。
- 边界/概要要求**同级相邻**才圈得出范围；圈不出来时如实报错并回喂给模型自己纠正。
- 删元素仍是**破坏性操作**：照旧先问用户一次。
- 写工具合计 **18 个**（第一批 10 + 第二批 8，守住「单批 ≤ 12」的纪律；数量用等值断言钉死）。
- 验证：自检 **1847 项全绿**（+46：意图解析、破坏性标记、标记 id 校验、缺 id 时的引导、
  闸门关闭时不下发、工具数量钉死，以及映射到的 4 个 store 动作本身——**幂等**、
  **整体替换**、空列表圈不出范围、都进撤销栈）。

### 新增 · 0.9.0-beta.1：许可激活入口（可分享给他人试用了）

- **常驻的激活入口**：以前只有"试用用完"之后，聊天面板里才露出许可码输入框——
  想**提前支持**的人在试用期里根本找不到地方激活（真被问过）。
  现在两处都能进：
  - **聊天面板的「试用剩 N 次 / Pro」徽标**本身可点 → 直接跳到 AI 设置；
  - **AI 设置里新增「许可（Pro）」一节**：常驻显示状态（试用剩几次 / 已激活（持有人））、
    粘贴许可码即可激活、已激活还能「取消激活」（换机器 / 退货都用它）。
- 许可仍然是**纯离线**验签（Ed25519），不发任何网络请求。
- 版本号 `0.8.0` → **`0.9.0-beta.1`**：`v0.8.0` 已经发布过，分享包不能与它同名不同内容。
- 打包产物（`npm run dist`）：`release/SMind-0.9.0-beta.1-x64-setup.exe`（安装版）、
  `release/SMind-0.9.0-beta.1-x64-portable.exe`（免安装，分享首选）。

### 新增 · 卡死的「黑匣子」：阶段心跳

- **为什么需要它**：同步死循环会**彻底堵死**渲染进程主线程——调试端口进不去、
  V8 采样要等进程正常退出才落盘（而卡死时只能强杀，等于什么都没留下）。
  于是加一条**在堵死状态下仍然有效**的线索：每秒把「当前在哪一步」写进控制台，
  主进程把它落进应用日志。**卡死时日志里最后一条心跳就是案发现场**。
- 已埋点：应用挂载完成 / 画布布局（前后）/ 镜头跟随 / 字体就绪后重算 / AI 第 N 轮 / 执行工具 X。
- 只在开发版运行（打包版不写日志、不跑定时器）。

### 修复 · 卡死不再需要强杀进程（10 秒自动恢复）

- **现象**：界面卡住后窗口连「关闭」都点不动，**调试命令也永远不返回**——说明主线程被
  **同步死循环**占住，而不是普通的慢（普通的慢每帧都会让出，调试命令进得去）。
  用户除了强杀进程没有别的办法。
- **对策（主进程自救）**：界面无响应超过 **10 秒**就自动重新加载界面，重载后弹一条说明。
  自救必须放在**主进程**——渲染进程被堵死时只有主进程还活着，这条路线一定走得通。
  文档每 30 秒有一份自动存档兜底；若中间自己缓过来则撤掉自救，不打扰。
- **黑匣子**：开发版可用 `SMIND_PROFILE=1` 启动，给渲染进程开 V8 采样；下次卡死会在项目根目录
  留下 `isolate-*.log`，`node --prof-process` 就能看到**卡在哪个函数**。
  （卡死时远程调试端口进不去，这是唯一能拿到调用栈的办法。）
- 另修：画布容器的 `ResizeObserver` 回调改为「尺寸真的变了才更新状态」，
  避免尺寸抖动被放大成自激重渲染。

### 修复 · 渲染进程烧满 CPU（「点击任何键都没反应、关也关不掉」）

- **现场**：应用日志里连续 `unresponsive | 渲染进程无响应`，任务管理器里**渲染进程烧掉几十秒 CPU**
  （其它进程只有 1 秒上下）——不是 AI 在干活，是界面里有个循环停不下来。
- **重点嫌疑**：「视角锁定」的跟随循环。它是「每帧向目标逼近 22%」，只有走到亚像素
  （`< 0.5px`）才收手；**只要有一处不收敛**（几何每帧都在变、视口尺寸在震荡、pan/zoom 变成 NaN），
  这个判断就永远为假 → 永久烧 CPU。而 `defaultViewLock` 默认是开的，所以从启动就在跑，
  重启也躲不掉。
- **修法（双保险）**：① 帧数止损——240 帧内不收敛就停手，并把现场（`focusKey`、`pan`）写进日志；
  ② 依赖每秒重跑超过 60 次（说明有东西在震荡，跟随会自我驱动成死循环）就暂停跟随。
  宁可不自动跟随，也不能把窗口卡死。
- **卡死时的出路交给主进程**：连续两次「无响应」会弹一个**原生对话框**，可以直接「重新加载界面」——
  渲染进程卡住时原生对话框照样能点，这是那时唯一还能点的按钮。
- **诊断增强**：渲染层的 `console.warn/error` 现在会写进应用日志，下次翻日志能直接看到是哪个模块在刷。

### 修复 · 卡死（点击任何键都没反应、关也关不掉）

- **根因**：工具队列是**同步递归**跑的（`queue.index += 1; step()`）——模型一次批量发几十个
  工具调用时，全部画布写入 + 重排会在一次调用栈里跑完，渲染进程主线程几分钟不回。
  应用日志里留下的就是连续的 `unresponsive | 渲染进程无响应`。
- **修法**：每个调用之间**让出一帧**（`setTimeout`），界面能持续重绘，Esc 与「停止」也真正生效；
  状态行顺便显示进度（`正在改画布…（12/40）`）。
- **「停止」现在真的能停**：以前只取消了网络请求，已排队的工具调用还会继续改画布——
  用户按了停止、画布却还在变，比不给停更让人生气。现在停止会清空队列并把回合收干净（撤销照样一下回退）。
- ⚠️ 卡死时窗口无法响应任何按键（这是主线程被占住的必然结果），只能强杀：
  `taskkill /F /IM electron.exe`。

### 修复 · `对话内容无效` 的真凶（批量调用被误判）

- **症状**：批量整理大文档时突然弹出 `Error invoking remote method 'ai:chat-stream': Error: 对话内容无效`，
  整回合直接死掉，重发同样的指令还会再犯。
- **根因**：主进程的入参校验里写着「单条消息最多 20 个工具调用」——而大文档批量删除/改名时，
  模型一次回复发几十个调用很自然（提示词里还鼓励它批量发）。撞上就被判成「内容无效」。
- **修法**：额度改成**只拦垃圾**的量级（单条消息工具调用 20 → 200、正文 6 万 → 100 万字），
  并且所有「对话内容无效」都改成**能定位**的报错（第几条、role、content 类型、toolCalls 个数、哪个字段不对）。

### 新增 · 开发版状态栏显示「构建 HH:MM:SS」

只为回答一个问题：**眼前这个窗口是不是旧的**。开发期最气人的情况是改了代码、窗口还停在两小时前
那份，用户在旧界面上复验、白折腾一轮（今天就踩了一次）。构建时间由 `electron.vite.config.ts` 注入，
只对开发版显示。

### 修复 · AI 聊天「看不出它在干活」

- **在聊天里选中文字按 Ctrl+C 复制不出来**：全局快捷键无条件 `preventDefault`，把 Ctrl+C
  一律当成「复制画布选中的节点」，于是面板里的文字永远复制不下来——用户报的
  「**无法从会话粘贴东西**」根因就在这儿（复制不出，当然粘不了）。
  现在页面上**真的有文字选区**时一律交还浏览器（画布节点不可选中，所以有选区必然是面板里的字）。
- **分不清它是还在想、还是已经答完了**：新增「正在工作」指示——转圈 + 一行说明
  （`正在思考…` / `正在翻看导图…` / `正在改画布…`，多轮时标出第几轮），
  流式回答末尾还会带一个**闪烁光标**，一眼看出这条还没写完。

### 修复 · 画布体验（折叠视角 / 关系线换行）

- **折叠 / 展开不再把视角带走**：点折叠时，被点的那个主题**留在屏幕原处**（反解平移量），
  画面不再整体跳走。更关键的一处：选中的主题若正在被折叠的那一支里，它已经从布局里消失了，
  视角锁定再也盯不到它——用户看到的就是「锁定突然失效、画面不跟了」；现在会把选择
  **提到折叠节点自己身上**（既看得见，锁定也能继续跟）。
- **关系线 / 边界标题支持手动换行**：以前只有概要能换行，这两种是单行 `input` 编辑 +
  单行 `<text>` 渲染，用户根本没处按回车。现在三者统一走 textarea
  （**Enter 换行、Esc 取消、Ctrl/Cmd+Enter 提交**、失焦也提交），渲染按 `\n` 拆行并整体垂直居中，
  尺寸沿用已有的多行估算；右侧属性面板里的关系线 / 边界输入框也一并打开多行。

### 修复 · AI 聊天面板（粘贴 / 消耗显示 / 停止标记）

- **便捷粘贴**：输入框旁新增「粘贴」按钮——走主进程读剪贴板（比渲染层 API 稳，无焦点/权限也不抛），
  内容插到光标处、保留换行、粘贴后光标落在内容末尾。输入框里的 Ctrl+V 原生路径不受影响。
  注意：**菜单「编辑 → 粘贴主题（Ctrl+V）」是画布命令**，粘的是主题节点不是文字——
  在聊天输入框里粘贴请用 Ctrl+V 或这个新按钮。
- **token 消耗显示**：每次回答底部显示本次消耗（问 / 答分列，多轮工具调用**累计**）；
  面板头部显示会话累计，清空对话时归零。数字一律按**服务商回报**统计，服务商不支持就不显示——
  **不做估算**（估出来的数字是编的）。实现：请求带 `stream_options.include_usage`；
  流末那个 `choices` 为空、只有 usage 的分片必须收下（以前会被当垃圾丢掉）。
- 「（已停止）」不再重复显示两次（内容为空时气泡标记与文案各写了一遍）。
- **试用徽标改为「试用剩 N 次」**：原来的「N / M」写法太像次数上限，用户会误读成
  工具调用限额（真被误读过了）；商业化那一侧的写回合上限照旧由 `TRIAL_TURN_LIMIT` 派生。

### 新增 · 商业化基建（Pro 闸门 / 离线许可 / 30 回合试用）

- **Pro 闸门放在主进程**：由主进程决定**下发哪些工具定义**——免费用户的对话里
  **一个写工具都不存在**，模型物理上调不动它（不是靠提示词拦，也不是渲染层判断；
  这也是提示词注入的最终防线）。
- **离线许可**：Ed25519 签名 + 内嵌公钥本地验签，**不联网、不做在线激活**（符合本地优先）。
  许可码形如 `SMIND1.<内容>.<签名>`，粘进「AI 面板 → 输入许可码」即生效；
  粘贴时带上的换行、空格、全角句点/横线都会自动清掉（不然用户只会看到"签名对不上"）。
- **30 个写回合试用**：只读聊天永久免费且**不计数**——只有真的动了画布的对话才算一个回合，
  计数在主进程（渲染层只负责显示）。用尽后面板显示剩余 `0/30` 与输入许可码入口；
  **只读聊天、以及 0.7/0.8 已交付的一切（含润色/展开/生成三个对话框）一概不受影响**
  ——这是 README 承诺的另一半边界。
- 提示词按许可状态如实切换：不能写时明确告诉模型「写工具没有下发」，并禁止它假装已经改了。
- **自动更新（W3，开发侧的最后一件事）**：打包版启动 45 秒后后台检查更新，**下载在后台、
  安装放在退出时**——不打断正在画图的人；「帮助 → 检查更新…」主动检查并把结果说清楚（包括失败原因）。
  更新渠道（GitHub Releases 的 owner/repo）与代码签名（Azure Trusted Signing）的配置位已留在
  `electron-builder.yml` 并写明步骤：**填上即生效，两者互不阻塞**（签名管 SmartScreen 信誉，
  自动更新只管拿到新版本）。开发版不检查更新。
- **签发工具**（只给项目所有者）：`npm run license:keygen` 生成密钥对、
  `license:issue -- --to 名字 [--order 单号]` 签许可码、`license:selftest` 检查
  「私钥与客户端内嵌公钥是否配套」（专防换了密钥忘粘公钥）。
  私钥写在**仓库之外**（`%USERPROFILE%\SMind-keys\license-private.pem`，可用 `SMIND_KEY_DIR` 改），
  请离线备份；仓库已忽略该文件名做第二道保险。
- 自检新增 **51 项断言**：许可码格式与清洗、**真签真验的 Ed25519 往返**（改内容、换公钥都必须验不过）、
  试用算术（不越界）、写回合判定、状态视图、工具闸门（不能写时一个写工具都不下发）。

### 新增 · 让 AI 动手改图（三期二期，进行中）

- **不再要求用户「先去画布上选中」**：实测模型会把「目标不明确」直接推给用户
  （「请先在画布上选中要细化的节点」），而那个节点标题其实就在**它自己上一条回复里**。
  三处修：① 系统提示词明确「**绝不要反过来要求用户去画布点选**」——标题用 `searchNodes`
  自己找；用户说「改 / 继续 / 动手」这类**复述式指令**时，指的就是你上一条回复里提到的主题；
  ② 应用把**用户这句话里提到的节点**（按标题自动匹配，带句柄与路径）直接注入提示词，
  模型连搜都不用搜；③ 工具结果就地提醒用句柄。
- **「说要改、其实没落地」的三处兜底**：① 写操作**失败也留痕迹**（面板显示「未执行：原因」），
  以前只有成功才有条目，用户看不出哪步没做；② **零写操作但话里带「已改好 / 已完成」这类结果声明**
  时，气泡上如实标注「本轮没有任何改动落到画布上」；③ **空操作不再报成「已执行」**
  （改同名、把已在末尾的节点再移到末尾 → 如实说「没有改动」；同一父级下的**重排仍然放行**）。
  提示词同步加规则：只有工具真的返回「已执行」之后，才可以说「已经改好了」。

- **针对「一次回复只发一个工具调用」的模型做适配**（qwen-plus 这类，用户实测「走一步推一步」）：
  ① 请求带 `parallel_tool_calls: true`，明确允许并行调用；
  ② 系统提示词加规则：一次回复可以带多个调用、同一批操作用**一条** `moveTopics` 发完、
  两次调用之间不写解说文字；
  ③ **工具结果里就地提醒**——模型单发 `moveTopic` / `renameTopic` 时，回喂里加一句
  「剩下的同类操作用 moveTopics 一次批量发出来」；在它当下的决策点纠正，比在提示词里讲一遍有效得多；
  ④ 轮数上限 8 → **16**、调用上限 50 → **60**（单发模型需要更多轮；安全阀仍在，只是不再误伤）。
- AI 设置补上选模型建议：改画布优先用支持工具调用的强模型；8k 级上下文装不下 agent 回合。

- **防「照抄已有内容」把画布复制一份**：整理 / 归类时模型很容易改用 `insertSubtree`
  把已有节点「重写一遍」——那是**新增**，等于复制（实测出过事故：整理后画布上出现了好几套
  相同的分类与节点）。现在两道防线：**硬拦**（outline 里 ≥5 个标题且过半数在文档中已存在时
  直接拒绝，并指引改用 `moveTopics`；确实要新增同名内容须带 `allowDuplicate: true`）+
  **软约束**（工具描述与系统提示词都写明「已有内容一律用 moveTopics 移动，不要重写」）。

- **读工具给模型发「句柄」，大导图整理可以**一回合完成**：大导图里同名标题能出现几十次
  （「创建 socket.socket()」这类），还有超长标题、带斜杠的标题——按标题寻址**不可能唯一**，
  模型只能反复试探（实测 256 节点整理搬了 69 个就卡住）。
  现在 `getSubtree` / `searchNodes` 的每行都带 `[#xxxxxx]` 短句柄，`address` 可直接填句柄：
  重名 / 超长 / 斜杠一律不再是障碍。系统提示词同时写明整理的标准三步
  （一次读到位拿句柄 → 建分类 → 按句柄批量搬完），并明确「不要回头再问用户用哪种方式」。

- **标题里带斜杠的节点不再是 AI 的盲区**：编程笔记里「class A: /A ()」这类标题很常见，
  以前路径解析一失败就直接报错、永远走不到「整串标题精确匹配」这一步——这类节点对 AI
  **完全不可见**（改名、移动全部失败；python 笔记整理实测卡在 3 个带斜杠的标题上，
  模型绕了好几轮都没绕过去）。现在路径走不通会先落回整串标题精确匹配（唯一才接受，
  重名仍然问清），救不回来才报路径错误。

- **「继续」不再失忆**：对话历史里只有模型说的**文字**、没有它做过什么——用户说「继续」时
  模型会从零开始重新读取、重新规划，大导图上就是同一种折腾反复来（实测 100 节点整理体验差的主因）。
  现在把上一轮的实际改动（工具摘要）注入 system prompt，并明确禁止重读重做。
- **整理任务写进行为规则**：直接动手（`insertSubtree` 建分类 + `moveTopics` 批量搬），
  不要在回复里贴「建议的大纲」让用户自己抄；做完只留一两句总结。
- **`moveTopics` 改为跳过容错**：100 条里错 1 条就整批退回 = 白烧一整轮；
  现在能执行的执行、失败的精确列出（第几条、什么原因），全都不行才整体报错。

- **批量移动 `moveTopics`（第 10 个写工具）**：整理大导图（上百个平铺节点）靠它——
  一次调用搬运一批节点，**调用次数上限按「调用」算、不按「节点」算**；
  规划阶段**全有或全无**：任何一条 address 解析失败都整批退回、并指出错在第几条（`moves[3]`），
  部分落空也如实回报「成功 x/y」。上限相应放宽到 **8 轮 / 50 次调用**——
  不去掉上限：没有它，模型陷入循环会无限烧用户的钱；有了批量工具，逐个搬本来就不该发生。
- **气泡版式改为「过程在上、结论在下」**：工具调用痕迹在上、AI 的回答压轴——
  以前回答在最上面、被工具条目和上限提示压住，用户找不到「回复」在哪儿。
  撞上限后的总结现在明确要求列出「已完成 / 未完成 / 建议下一步（例如再说一句「继续」）」。

- **改完看得见**：回合结束时，AI 动过的节点会**闪两下**、视口自动带到第一处改动
  （直接操作没有「预览确认」这一环，信任全靠这一眼）。
- **不丢用户正在输入的字**：AI 动手前先把编辑框里未提交的标题按正常流程提交，
  且这次提交在 AI 事务之外——`Ctrl+Z` 先撤 AI 的改动、再撤这次提交，顺序对得上。
- **不搅乱用户摆好的版面**：手动摆过位置的主题（画布上的自由节点）默认**不许 AI 移动**；
  确实需要时模型必须显式带 `allowMoved: true` 再来一次。

- **AI 能直接改画布了**：新增 9 个写工具——改名 / 插入子树 / 删除分支 / 移动 / 折叠 / 备注 / 代码块 / 公式 /
  提问（`askUser`）。工具全部映射到**已有的、经过验证的 store 动作**，AI 与用户快捷键走同一条路。
- **一次命令 = 一步撤销**：整轮 AI 的改动在结束时并成**一步**，`Ctrl+Z` 一下全回来；
  回合进行中用户级的撤销 / 重做被临时锁住（否则从中间抽走历史会让后续步骤错位）。
- **破坏性操作先问人**：删分支这类操作会在面板里停下来等用户点「执行 / 跳过」；
  跳过会**如实回喂**给模型（否则它会以为已经删了，后面的判断全错）。
- **动手前先存档**：第一笔写操作前自动存一份版本快照——撤销栈在内存里，崩溃就没了。
- **改了什么说得清**：气泡里以小标签列出每一步，收尾再给一行摘要
  （「已改动：…（共 N 处，Ctrl+Z 一次全部撤销）」）。
- 自检新增 **48 项断言**：写工具的参数校验与寻址、事务合并规则
  （含「换父移动 + 改名」这个当年坏过数据的组合）、单实例心跳。

### 修复

- **气泡里只剩一个思维链标签**：推理模型把思维链一起塞进正文时，回答会夹带「think 标签」，
  有时甚至只剩一个**闭标签**孤零零挂在气泡里（服务商把开头那段放进了 `reasoning_content`）。
  现在流式层统一过滤思维链——标签被分片切开也认得出来，被切开时先留住尾巴不显示半截标签。
- **「已达上限」重复提示三次、最后什么都没等到**：撞上限后的「不带工具的决胜轮」以前会反复触发
  （模型还要工具 → 再问一次 → 又撞上限……）。现在决胜轮**只允许一次**，模型再要工具就硬停，
  并在气泡里如实说明原因。
- **读取子树失败**：模型把**文档名**也写进路径开头时一律解析不了（真事：`AI 测试Mind/CART树/…`）。
  现在允许跳过开头一到两段；失败时还会把「这一层有哪些子主题」列给模型，让它下一步自己纠正。
- **AI 造出「新主题」垃圾节点**：模型一次给出多行**并列**内容时，大纲解析器会套一个壳节点，
  而写工具把这个壳直接落进了画布 → 画布上凭空多出一个叫「新主题」的节点。
  现在解析器会标记「根是不是壳」，写工具**把壳剥掉**，让那些行成为并列的新主题（工具描述同步改成这个语义）。
- **撞到工具调用上限就不作答**：以前到上限直接收尾，用户拿到的是半截话。现在上限只当安全阀——
  撞上限时**去掉工具再问最后一轮**，逼模型把已经看到的东西讲清楚（并修正了「助手消息带 `tool_calls`
  却没有工具结果」这个会让服务端直接报 400 的协议顺序问题）。
- **重复调用白烧配额**：同一回合里同名同参数的调用不再重复执行，直接告诉模型「问过了，换个策略」；
  上限也从 6 轮 / 20 次放宽到 **8 轮 / 30 次**（读一篇大文档常常要十几次探查）。
- **撤销栈跨重启会清空**（这是内存里的栈，重启就没了）：AI 改动的摘要里现在写清两条恢复路径——
  按一次 `Ctrl+Z` 整体回退；已保存的文档还会提示「动手前的状态已存进版本快照（`Ctrl+H`），重启过也能回到那里」，
  未保存的文档则如实说明没有快照、重启后无从回退。
- **渲染层错误现在会落盘**：以前只打在终端（`console.error`）里，应用一重启就查不到了；
  而且错误边界**接不住**异步 / 事件里抛出的错误（那类问题连提示都没有）。
  现在错误边界与新增的 `window error` / `unhandledrejection` 兜底统一上报主进程，
  写进 `%APPDATA%\SMind\logs\`。
- **被强杀之后再也启动不了**（双击图标毫无反应，也没有任何提示）：上一次异常退出会留下
  残留的单实例锁，之后每次启动都被判成「已有实例在运行」而静默退出。现在加了**心跳**：
  只有确实有活着的实例在跑才退出，心跳过期就清掉残留锁、重试一次。
  （「启动干干净净、然后什么都没了」是排查时最难查的一类现象。）

## [0.8.0] - 2026-09-15

0.8 的主题是**把 AI 请进软件里**：一个能自己翻看导图、用自然语言回答的助手（免费、只读）。

### 新增 · 内置 AI 助手（聊天面板 + 只读探查工具）

- **新增 AI 聊天面板**：右侧抽屉，工具栏「AI → 打开 AI 聊天…」进入，用自然语言聊当前导图。
- **流式输出**：主进程把 SSE 增量逐块转发给渲染层，回答边生成边显示；可随时「停止生成」，
  已生成的部分保留（不丢）。
- **上下文**：每次提问时**重新读取**「骨架摘要（中心主题 + 一级分支 + 各自节点数）+ 当前选中路径 + 文档规模」；
  万级节点也只有几百 token，且提示词把易变内容放在后面，吃满服务商的 prompt 缓存。
- **只读**：本版本不改画布，AI 只给建议——零数据风险；直接改图的能力在二期（写工具 + 事务安全网）。
- 未配置 Key 时面板给引导；**对话记录按文档保留**（存应用数据目录 `chat/`，用文档路径的哈希做文件名；
  **不写进 .xmind**；未保存的文档只在内存里，关掉即弃）。
- **回复里的节点引用可点击**：点一下就在画布上选中并居中（按首字分桶建索引，大文档也不卡；长标题优先，
  不会被短标题截断）。
- **AI 可以自己翻看导图了（1b）**：注册 4 个**只读**工具——`getSelection`、`searchNodes`、`getSubtree`、
  `getDocStats`；模型按需调用、结果回喂后继续作答（一轮最多 6 个来回 / 20 次调用，超限会**明确告知**
  而不是静默停下）。
- **工具调用循环是自己实现的一层**：流式 `tool_calls` 分片累积（参数是逐片拼起来的字符串，
  覆盖式赋值只会拿到半截 JSON）、按 OpenAI 协议转换消息线（`assistant.tool_calls` + `role: tool` +
  `tool_call_id`）、工具失败**不中断对话**而是把可读原因回喂给模型自我纠正；
  模型不支持函数调用时自动降级为纯对话并在面板上说明。
- **寻址由应用负责**：模型只给字符串（id / 标题路径 / 唯一标题），解析成节点是本地代码的事；
  重名标题**拒绝硬选**并列出候选路径——「自信地改错节点」比报错更糟。工具结果带上限截断，防上下文被撑爆。
- 面板里以**工具痕迹**（小标签）显示 AI 翻看过什么，用户看得见它做过的事。
- 输入法回车不误发（`isComposing` 判断）、Enter 发送 / Shift+Enter 换行。

### 修复

- **点「停止生成」后整块界面崩溃**（`Cannot read properties of undefined`）——
  收尾时用展开写法 `{ ...消息, content: undefined }` 把内容清成了 undefined，下一次渲染读它的长度就崩。
  现在补字段只覆盖**真正给了值**的键（`patchLast`），并在节点引用切分处加了非字符串防御：
  坏数据最多让这段不高亮，不会再把界面带崩。（另：这次崩溃被 0.7 加的**错误边界**接住了，
  没有变成白屏——兜底按预期生效。）
- **错误边界下窗口关不掉、且「重新加载界面」点了没反应**——
  两个根因：① 关闭请求只由 `App` 响应，而错误边界恰好把 `App` 卸载了，没人回应，主进程就一直等；
  ② `will-navigate` 里一刀切 `preventDefault()` 把**刷新**也拦了。
  现在：界面损坏时主进程**跳过未保存确认直接允许关闭**（并记日志）、刷新改由**主进程发起**
  （`window.api.reloadWindow()`）；`will-navigate` 只拦「不是回到自身页面」的导航
  —— 拖进来的图片/PDF 仍会被拦下，原有保护不变。

### 自检

- 新增 **91 项断言**：骨架摘要、系统提示词、SSE 分片解析（半截行缓冲 / CRLF / 心跳干扰）、
  节点引用切分（长标题优先）、聊天记录校验（坏条目丢弃 / 条数上限 / 超长截断）、
  工具调用分片累积（跨片拼参数）、请求体线格式、寻址解析（重名不硬选）、4 个只读工具、循环上限。

## [0.7.0] - 2026-09-15

### 安全与健壮性

- **保存改为原子写**：先写同目录临时文件 → `fsync` → `rename` 覆盖。
  此前是就地覆盖，写到一半被打断（磁盘满/崩溃/断电）会把用户原稿截断成坏包。
- **新增错误边界**：渲染期异常不再白屏，给出错误详情、复制按钮与重新加载入口。
- **新增主进程崩溃兜底**：`uncaughtException` / `unhandledRejection` / `render-process-gone`
  都会落日志到 `%APPDATA%\SMind\logs\`，菜单「帮助 → 打开日志目录」可直接打开。
- **自动保存失败不再静默**：会明确提示"自动保存失败，请尽快手动保存"。
- **损坏文件提示**：压缩包损坏时给出中文说明，而不是 JSZip 的英文异常。
- **生产构建注入 CSP**（`script-src 'self'`、禁 `object` / `base` / `form`）。
- **渲染进程开启沙箱**（`sandbox: true`，此前为 false）。
- **IPC 入参校验**：路径合法性、图片 20MB 上限（`shared/ipc-args.ts`）。
- **外部字段逐字段校验**：富文本 run 与代码块不再用 `as` 强转，颜色/字体限定字符范围。
- **公式报错消息转义**后再拼进 HTML 属性。

### 性能

- **测量缓存新增按对象身份的快速路径**：编辑时每敲一个字都会重跑全量布局，
  未改动节点此前仍要逐个重建缓存键，现在直接命中。
- **缓存淘汰改为"淘汰最旧一批"**（此前到上限整体清空，会造成周期性卡顿尖峰）。
- **搜索防抖** 180ms（画布高亮与搜索面板共用）。
- **大纲面板**禁用视口外行的布局与绘制（`content-visibility`）。
- **筛选算法**去掉每层递归都复制路径数组的开销。
- **修复**：改「默认对齐 / 代码块基准字号」后布局不重算的问题。

### 工程

- 接入 **ESLint（flat config）+ Prettier + EditorConfig**，`npm run lint` 零 error / 零 warning
  （脚本带 `--max-warnings 0` 锁住基线）；顺带修掉 hook 依赖导致的闭包读到旧值问题。
- 接入 **CI**（`.github/workflows/ci.yml`）：类型 + 规范 + 自检 + 样本往返。
- tsconfig 增加 `noUnusedLocals` / `noUnusedParameters`。
- 新增 `CHANGELOG.md`、`CONTRIBUTING.md`、`SECURITY.md`、`docs/known-issues.md`。
- 收紧零散实现：外部数据收敛（`shared/model/coerce.ts`）、缓存淘汰（`shared/cache.ts`）、
  原子写（`main/atomic-write.ts`）、编辑态（`NO_EDITING` + `editingContent`）。
- 修复：工具栏「关系线 / 概要 / 边界」三个按钮**丢失了禁用原因提示**（变量算了却没用上）。
- **全仓启用 `noUncheckedIndexedAccess`**（`tsconfig.strict.json`，覆盖 **94 个源文件**），
  修掉约 200 处"可能取到空值"——其中数处是真会崩的：AI 返回内容里没有大纲行时、
  对话框被取消却仍去取第一个文件时、公式尺寸算不出来时、导出图片像素越界时、
  空工作簿取第一张画布时、Markdown 正则分组缺失时。
  修法一律按语义处理（提前退出 / 合理兜底 / 确证才断言），不做机械 `!`。

### 其他

- 应用图标字母由 **M** 改为 **S**（产品名 SMind），重新生成 7 个尺寸的 ico 与各档 PNG。
- README 修正「同时只能开一个窗口」的过期说明（多窗口早已实现），并补上开发相关链接。

## [0.6.0] - 2026-09-14

- 设置分发到子位置（工具栏 / 主题 / 节点 / 渲染默认值各归其位）
- 工具栏「更多 ▾」里的功能可以拿出到快捷栏，也能右键收回
- 代码块默认语言与默认字号可选
- 修复：长连线消失；右键收纳接线补齐

## [0.5.0] - 2026-09-14

- **多文档标签**：底部任务栏式标签页（浏览器式多文件）
- 上下标：紧贴写法（`a^1^` / `a~1~`）生效，格式栏按钮支持光标处切换
- 折叠徽标改为圆形简约样式（保留数字）
- 修复：套用默认主题时若已经是该主题，不再误记为改动（启动即显示 ● 未保存）

## [0.4.1] - 2026-09-14

- 公式不再被截断，公式节点外框有下限
- **手动拉伸节点**（右下角手柄 + 尺寸下限语义 + 往返保真），拉伸时图片等比跟随
- 设置中心：默认视角锁定 / 默认主题 / 默认对齐
- 标记竖排到节点外侧，折叠按钮显示折叠数量
- Markdown 导出保真（与导入器构成往返），布局无重叠全局断言
- 公式与代码块入口上快捷栏

## [0.4.0] - 2026-09-13

- **图片节点**：`Ctrl+V` 贴截图、拖图片文件、清空标题即成纯图节点
- **代码块节点**：多语言、等宽排版、导出时一并绘制
- **Markdown 导入**全格式提取
- 分支级结构补全：矩阵 / 时间轴 / 括号 / 树状表格全部到位
- 修复：分支上切换结构此前不生效（只读中心主题的结构）

## [0.3.0] - 2026-09-13

- **产品名由 Mind 改为 SMind**（作者署名 Wentun）
- 许可证改为 **PolyForm Noncommercial 1.0.0**：自用 / 学习 / 公益组织可自由使用，商用需另行授权

## [0.2.3] - 2026-09-12

- **从文件管理器打开**：双击 `.xmind`（安装版注册关联）、拖到 exe、命令行参数

## [0.2.0] - 2026-09-12

- 视角锁定（镜头跟住选中的主题，逐帧缓动）
- 选中后直接打字即进入编辑；输入法首字母不再被当成普通字符
- 修复：编辑态长文本边打字边换行，不再撑破节点

## [0.1.1] - 2026-09-12

- 打包复打与分发说明

## [0.1.0] - 2026-09-12

首个可用版（P0 + P1）：

- Electron 桌面外壳、原生菜单、文件打开与保存对话框
- 无限画布：平移、缩放（10%–400%）、适应画布、指针锚点缩放
- 节点编辑、多行文本、自动换行与自适应尺寸
- 布局：思维导图（左右平衡）与逻辑图（向左 / 向右，新建默认）
- 拖拽改变父级、拖到空白处自由摆放、恢复自动布局
- **以 `.xmind` 为原生格式**双向兼容，未知字段原样保留
- 基于 Immer patches 的无限撤销重做（上限 200 步）
- 每 30 秒自动保存、崩溃恢复提示、关闭前未保存确认
- 视口裁剪、文本测量缓存、TypeScript 全量类型检查
- `samples/` 下 21 个真实 `.xmind` 样本
