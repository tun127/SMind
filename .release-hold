{
  "version": "0.9.5",
  "frozenAt": "2026-09-22",
  "assetsDir": "release-frozen-0.9.5",
  "policy": "递延一版再发（用户 2026-09-22 拍板）：功能版冻结后先不发，等下一个版本冻结时再发本版。本版（独立主题功能线）等 0.9.6 冻结之后再发。",
  "releaseAfter": "0.9.6 冻结",
  "unlock": "正常发布本版时删除本文件；应急可设 SMIND_RELEASE_FORCE=1 强制上传。",
  "why": "package.json 此刻已是 0.9.5（未发），而线上服务的是已发布版本 —— 没有这道闸门，任何一次 npm run mirror:oss 都会把持有版直接发给用户。",
  "note": "修复版（hotfix）不适用递延：0.9.4 是修复线，从 0.9.3 发版点切出、冻结后即发布，见台账 #26。"
}
